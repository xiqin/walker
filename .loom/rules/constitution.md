# 项目宪章

> 本文件由 `/loom-init-project` 自动生成，请根据项目实际情况完善。

## 编码行为准则

### 1. 先思考，再编码

- 显式声明你的假设，不确定时主动提问
- 当存在多种合理解读时，列举所有可能性，不要默默选择一种
- 如果有更简单的方案，必须提出

### 2. 极简优先

- 在掌控全局的情况下，只写解决问题的最小代码，不做推测性实现
- 不添加用户未要求的功能、配置项或抽象层
- 判断标准：一个高级工程师看了会不会觉得过度设计？

### 3. 精准手术

- 只改动必须改动的部分，不"顺便优化"相邻代码
- 保持现有代码风格，除非用户明确要求改变
- 清理范围仅限于自己改动产生的孤代码

### 4. 目标驱动

- 将模糊任务转化为可验证的成功标准
- 制定多步验证计划，循环执行直到达标
- 不声称"完成"，除非验证通过

### 5. 合理注释

- 每个function添加注释，描述function的定义和参数说明
- 在复杂逻辑出添加注释说明
- 使用中文。

## 开发流程

所有 skill 执行步骤和下一步流转以 `.loom/workflow.yaml` 为准，不要自行决定顺序。

## 核心原则

1. **遵循现有架构边界**：新增代码放在既有分层中，不为单次需求创建额外架构层。
2. **依赖显式传递**：优先复用项目现有依赖注入方式，避免隐藏全局状态。
3. **配置集中管理**：新增配置必须进入项目既有配置系统，并提供安全默认值。
4. **错误可诊断**：保留调用上下文，面向用户和日志使用一致的错误语义。
5. **生成物可追踪**：生成代码必须有来源、命令和再生成方式。

## 技术栈

| 技术              | 版本                  | 用途     |
| ----------------- | --------------------- | -------- |
| [TODO: 编程语言]      | [TODO: 语言版本]  | 编程语言 |
| [TODO: Web 框架] | [TODO: 版本] | Web 框架 |
| [TODO: ORM]           | [TODO: 版本]       | ORM      |
| [TODO: 数据库]      | [TODO: 版本]  | 数据库   |
| [TODO: 缓存]         | [TODO: 版本]     | 缓存     |
| [TODO: 日志]       | [TODO: 版本]   | 日志     |
| [TODO: 依赖注入]            | [TODO: 版本]        | 依赖注入 |

## 架构模式

```text
[TODO: 架构模式]
```

## 目录结构

```text
walker
```

## 编码红线

> 以下红线为自动生成，请确认并补充。

- [TODO] 补充项目禁止事项、兼容性边界和安全红线。

## 项目约束

- 语言版本：[TODO: 语言版本]
- 必须通过：[TODO: 构建命令]
- 必须通过：[TODO: 静态检查命令]
- 测试命令：[TODO: 测试命令]

## 治理规则

本章程高于所有其他开发规范。

- 修订章程必须记录变更内容、获得审批。
- 所有合并请求必须验证是否符合本章程。
