# Project Memory

> Auto-generated by `loom memory export`. Do not edit — use `loom memory add` instead.
> Last exported: 2026-07-20 01:43

## 📌 摘要

2026-07-20 | 踩坑 | node:test 父套件层 Promise resolution is still pending 会被标记 cancelledByParent 导致子测试显示失败；独立运行同一文件则 0 fail。诊断时须先单独运行该测试文件确认子测试本身是否通过。
2026-07-20 | 决策 | 原生 question 飞书映射：独立映射 OpenCode question.asked/replied/rejected，不复用 permission 事件；一次请求多题逐题发飞书卡片，全部答案齐备后一次性 question.reply answers: string[][]；TUI bridge 升级 protocol v4，旧 runtime 返回 QUESTION_REPLY_UNSUPPORTED；控制 delivery 可在 session busy/父 prompt leased 时并行执行，同 session 最多一个控制 delivery；accepted 确认 10 秒超时主动收敛；回复失败按 retryable/succeeded/processed_unknown 分类，仅 safeToRetry:true && sdkInvoked:false 才重试；终态 24h/1000 条有界保留。
2026-07-17 | 偏好 | permission 与 question 路由隔离：data.type=question 走 /answer 命令和 handleQuestion/handleQuestionReplied，传统 permission 走 /permit allow\|deny 和 handle/handleReplied；patch 卡片策略区分 transport（HTTP 直接 patch，TUI bridge 等 permission_replied 事件驱动）
2026-07-17 | 踩坑 | 飞书卡片 multi_select_static 和 input 组件类型在代码库中首次使用，需实际卡片预览工具验证渲染效果；buildQuestionCard options.value 非空校验由 dispatcher _cmdAnswer required 校验兜底，卡片层未单独校验
2026-07-17 | 决策 | 飞书交互式问题确认功能：复用 permission 事件通道承载交互式问题，新增 /answer 命令、questionReplyStates 3态机、buildQuestionCard(4种inputMode)、replyQuestion delivery，与传统 /permit allow\|deny 分离
2026-07-16 | 决策 | 飞书 /model 模型目录采用原卡片分页：先按当前模型、Recent、配置模型、其余 provider 构造全局稳定去重序列，再按每页 20 个模型切片；导航使用 cmd:/model --page N，并通过卡片 messageId 调 patchCard 原地更新，分页动作绕过普通命令去重但不更新 session.model。
2026-07-16 | 踩坑 | loom-verification-before-completion 的 verify-artifacts.mjs 在当前环境缺失 C:\Users\tianxiqin\.config\opencode\src\core\artifact-checker.js，会报 ERR_MODULE_NOT_FOUND；本次以手工核验、npm run check 日志和 SHA-256 证据补足，项目代码本身验证通过。
2026-07-15 | 决策 | 飞书 /model 不再修改 OpenCode 全局配置，只更新当前 Walker session；切换前通过 driver.listModels() 验证并规范化为 {providerID, modelID} 对象。/new 继承当前 session 或 defaultModel 模型。TUI /clear 直接在 createSession 传 model 对象，避免二次 updateSessionField。session.model 统一为对象，读取边界兼容 string 但不做持久化迁移。
2026-07-15 | 踩坑 | TUI bridge clear 汇合状态机：_tryCompleteClear 中不能检查 runtime.currentSessionId !== oldSessionId，因为 clear 期间普通 register 合法更新 currentSessionId 后会导致 clear 误判失败。正确做法是仅依靠 controlDeliveryId 绑定 + control result 的 sessionId 一致性检查来保障两阶段提交正确性。另外 _preflightClear 中 _callFeishu 必须 await，否则 fire-and-forget 的 Promise reject 会产生未处理 rejection。
2026-07-15 | 决策 | 飞书 /clear 命令采用 TUI bridge 控制消息方案：当前 TUI 通过本地 SDK api.client.session.create() 创建新顶层 session 并 api.route.navigate() 导航，同 runtimeId 重新注册后更新原 route 焦点；旧会话保留，新会话继承旧 session.model 和 cwd；不调用 openTerminal，不改变 /new 行为。关联 register 携带 controlDeliveryId 精确绑定 clear pending，支持 control result 与 register 任意顺序到达，两阶段原子提交。

## 🏗 架构决策（ADR）

_No ADRs yet._

## ⚠️ 踩坑记录

- **2026-07-20**: node:test 父套件层 Promise resolution is still pending 会被标记 cancelledByParent 导致子测试显示失败；独立运行同一文件则 0 fail。诊断时须先单独运行该测试文件确认子测试本身是否通过。
- **2026-07-17**: 飞书卡片 multi_select_static 和 input 组件类型在代码库中首次使用，需实际卡片预览工具验证渲染效果；buildQuestionCard options.value 非空校验由 dispatcher _cmdAnswer required 校验兜底，卡片层未单独校验
- **2026-07-16**: loom-verification-before-completion 的 verify-artifacts.mjs 在当前环境缺失 C:\Users\tianxiqin\.config\opencode\src\core\artifact-checker.js，会报 ERR_MODULE_NOT_FOUND；本次以手工核验、npm run check 日志和 SHA-256 证据补足，项目代码本身验证通过。
- **2026-07-15**: TUI bridge clear 汇合状态机：_tryCompleteClear 中不能检查 runtime.currentSessionId !== oldSessionId，因为 clear 期间普通 register 合法更新 currentSessionId 后会导致 clear 误判失败。正确做法是仅依靠 controlDeliveryId 绑定 + control result 的 sessionId 一致性检查来保障两阶段提交正确性。另外 _preflightClear 中 _callFeishu 必须 await，否则 fire-and-forget 的 Promise reject 会产生未处理 rejection。
- **2026-07-15**: OpenCode TUI bridge 在 /new 后即使 promptAsync 成功且 Walker 日志显示 driver prompt completed eventCount>0，也不代表消息在当前 TUI route 可见。插件必须跟踪根 session.created/tui.session.select，并在投递目标 session 与 api.route.current 不一致时先调用 api.route.navigate('session', { sessionID })，再调用 promptAsync。
- **2026-07-15**: OpenCode 1.17.20 执行 /new 会通过 session.created 提供新根会话 ID；TUI bridge 插件不能只轮询可能滞后的 api.route.current。应维护事件驱动的 activeSessionId，监听 session.created 与 tui.session.select，并过滤带 parentID 的内部子会话。
- **2026-07-15**: OpenCode Hook 必须使用插件上下文 ctx.serverUrl 上报会话；固定 localhost:4096 会把本地 TUI 会话绑定到错误进程，且所有 session-scoped HTTP/SSE 必须尊重 sessionRef.serverUrl。
- **2026-07-14**: OpencodeSessionWatcher._resumePolling 必须传递 watcher._handlers 而非 { onEvent: watcher._handlers }，否则 _startMessagePolling 内 handlers.onEvent 为 undefined，导致 TUI 回复在 suspend/resume 后无法即时推送，必须等下一条飞书消息触发
- **2026-07-13**: hook receiver 路由 /opencode/hook/session-created 不在 admin router 的 isAdminApiPath 匹配范围（只匹配 /api/admin/ 前缀），导致 handleRequest 不进入路由分发，handler 运行时不可达。根因：新增非 /api/admin/ 前缀的路由时需同步扩展 isAdminApiPath。解决：router.js isAdminApiPath 增加 /opencode/hook/ 前缀匹配。教训：新增路由前缀时必须检查路由分发层的路径匹配逻辑，不能只依赖路由注册。
- **2026-07-10**: loom verification artifact checker script failed because local opencode skill install lacks src/core/artifact-checker.js; mitigated by fresh npm run check evidence receipt, placeholder scan, task-state check, and manual artifact verification.
- **2026-07-09**: 飞书 WSClient 参数兼容性：Lark SDK WSClient 构造参数使用 appId（非 appID），eventDispatcher 应传入 start() 而非构造函数；路径：src/platform/feishu/platform.js
- **2026-07-10**: server.js port=0回退bug：config.port \|\| 8787 将测试端口0回退为8787导致EADDRINUSE冲突；修复为 config.port != null ? config.port : 8787
- **2026-07-10**: 流水线占位符检查误报：自审句中含TBD/TODO字面量会触发占位符扫描阻断，需避免在自审/结论表述中使用这些关键字
- **2026-07-10**: 前后端API路径对齐是必须同步完成的：spec定义的API路径、HTTP方法和字段名必须与后端路由和前端app.js一致，否则功能不可用
- **2026-07-16**: OpencodeSessionWatcher 游标语义 bug：原实现在 prompt 超时/失败的 finally 中无条件把 _lastPolledMessageId 推进到消息列表最后一条（即使该 assistant message 仍是 pending），导致该 message 原地 completed 后因 ID 已是游标而被 polling 永久跳过，后续结果丢失。修复：游标语义改为 lastDeliveredCompletedAssistantMessageId，只在 prompt 成功完成时推进到 completed assistant message；transport 失败绝不推进到 pending；同 ID pending→completed 在后续 poll 可见可补投。

## 👤 用户偏好

- permission 与 question 路由隔离：data.type=question 走 /answer 命令和 handleQuestion/handleQuestionReplied，传统 permission 走 /permit allow\|deny 和 handle/handleReplied；patch 卡片策略区分 transport（HTTP 直接 patch，TUI bridge 等 permission_replied 事件驱动）
