# Project Memory

> Auto-generated by `loom memory export`. Do not edit — use `loom memory add` instead.
> Last exported: 2026-07-13 03:50

## 📌 摘要

2026-07-13 | 踩坑 | hook receiver 路由 /opencode/hook/session-created 不在 admin router 的 isAdminApiPath 匹配范围（只匹配 /api/admin/ 前缀），导致 handleRequest 不进入路由分发，handler 运行时不可达。根因：新增非 /api/admin/ 前缀的路由时需同步扩展 isAdminApiPath。解决：router.js isAdminApiPath 增加 /opencode/hook/ 前缀匹配。教训：新增路由前缀时必须检查路由分发层的路径匹配逻辑，不能只依赖路由注册。
2026-07-13 | 决策 | Session 路由从 1:1 升级为 1:N：routes 从 { routeKey: sessionId } 改为 { routeKey: { focusSessionId, sessions[], cwd, updatedAt } }。一个 routeKey（飞书群/线程/用户）下可绑定多个 OpenCode session，普通消息默认发给焦点 session，/use 切焦点，/list 列 route 下所有 session，非焦点 session 输出回群带 [session: <id前8位>] 标识。OpenCode 退出时每 session 独立心跳检测，detached 后从 route 移除并自动切焦点。同一 cwd 多个 OpenCode 通过 routeKey 不会串会话。
2026-07-13 | 决策 | OpenCode 自动纳入采用 Hook Plugin 方案（非 wrapper/shim）：Walker 启动时自动写入 plugin 到 ~/.config/opencode/plugins/walker-hook.js，plugin 监听 session.created 事件上报到 /opencode/hook/session-created 端点，Walker 按 cwd 匹配 routeKey 创建 Walker session 并纳入 1:N route 列表。安全约束：仅接受 loopback 请求 + 复用 admin token 鉴权。淘汰了原 wrapper 劫持 PATH 方案。
2026-07-11 | 状态 | Walker 短期演进已完成并验证：新增可配置飞书进度卡片心跳、/cancel、/status 与 /ps、WALKER_MAX_TURN_TIME_MINS 单轮超时取消，并补强取消/超时残留输出和重复推送防护；验证 npm test 504/504 通过，CodeGraph 已同步。
2026-07-10 | 踩坑 | loom verification artifact checker script failed because local opencode skill install lacks src/core/artifact-checker.js; mitigated by fresh npm run check evidence receipt, placeholder scan, task-state check, and manual artifact verification.
2026-07-10 | 状态 | project-health-sweep completed: hardened Feishu API/platform, JsonStore/HTTP/SSE, SessionService, MessageDispatcher, OpenCode driver, and Windows/WSL runtime boundaries; final verification npm run check passed with 192 tests.
2026-07-09 | 决策 | setInterval 保活方案：当前使用 setInterval(callback, interval) 作为进程保活 hack，审查建议优化为优雅保活方案（如 SIGUSR1 handler），暂保留现有方案待后续优化
2026-07-09 | 踩坑 | 飞书 WSClient 参数兼容性：Lark SDK WSClient 构造参数使用 appId（非 appID），eventDispatcher 应传入 start() 而非构造函数；路径：src/platform/feishu/platform.js
2026-07-09 | 状态 | 项目初始化完成
2026-07-10 | 决策 | Web管理端选型：方案A（在Walker单进程内增加AdminServer），复用现有service/driver/runtime/卡片渲染，默认127.0.0.1:8787，WALKER_ADMIN_ENABLED控制开关，token鉴权

## 🏗 架构决策（ADR）

_No ADRs yet._

## ⚠️ 踩坑记录

- **2026-07-13**: hook receiver 路由 /opencode/hook/session-created 不在 admin router 的 isAdminApiPath 匹配范围（只匹配 /api/admin/ 前缀），导致 handleRequest 不进入路由分发，handler 运行时不可达。根因：新增非 /api/admin/ 前缀的路由时需同步扩展 isAdminApiPath。解决：router.js isAdminApiPath 增加 /opencode/hook/ 前缀匹配。教训：新增路由前缀时必须检查路由分发层的路径匹配逻辑，不能只依赖路由注册。
- **2026-07-10**: loom verification artifact checker script failed because local opencode skill install lacks src/core/artifact-checker.js; mitigated by fresh npm run check evidence receipt, placeholder scan, task-state check, and manual artifact verification.
- **2026-07-09**: 飞书 WSClient 参数兼容性：Lark SDK WSClient 构造参数使用 appId（非 appID），eventDispatcher 应传入 start() 而非构造函数；路径：src/platform/feishu/platform.js
- **2026-07-10**: server.js port=0回退bug：config.port \|\| 8787 将测试端口0回退为8787导致EADDRINUSE冲突；修复为 config.port != null ? config.port : 8787
- **2026-07-10**: 流水线占位符检查误报：自审句中含TBD/TODO字面量会触发占位符扫描阻断，需避免在自审/结论表述中使用这些关键字
- **2026-07-10**: 前后端API路径对齐是必须同步完成的：spec定义的API路径、HTTP方法和字段名必须与后端路由和前端app.js一致，否则功能不可用

## 👤 用户偏好

_No preferences recorded._
