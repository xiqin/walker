# Project Memory

> Auto-generated by `loom memory export`. Do not edit — use `loom memory add` instead.
> Last exported: 2026-08-06 07:00

## 📌 摘要

2026-08-06 | 决策 | Claude TUI 跨 Walker 重启续接采用本地 bridge/sidecar 控制面：旧 runtime 可续接时飞书消息写回同一 runtime；不可用时才回退到 claude --resume 新受控 runtime；Walker stop 只释放 Walker connection，不等同用户 stop/delete。
2026-08-03 | 决策 | Claude/kscc provider 的 OpenCode 等价体验采用 Walker 层 cli-terminal/window 链路：/new claude 通过可注入 openTerminal 拉起可见 Claude/kscc 终端并记录 terminal metadata；飞书普通消息继续通过后台 claude --print --output-format stream-json 向同一 claudeSessionId 发送以复用上下文；不向可见 TUI 进程注入输入，也不伪造 OpenCode HTTP server、TUI bridge、sqlite 或 opencodeSessionId 语义。
2026-08-03 | 决策 | Claude provider 已按 Claude Code CLI 集成：使用 claude --print --output-format stream-json 作为真实 driver 传输层，不依赖未验证 Claude HTTP API，不伪造 OpenCode TUI/sqlite 能力，默认不传 bypassPermissions 或 dangerous skip 权限；自动测试使用 fake CLI 或注入依赖避免真实 Claude prompt。
2026-07-31 | 踩坑 | Loom verification/executing validators 对报告文本和证据格式较严格：test-report.md/verify-report.md 的 PASS evidence receipt 需要行级字段 evidence-command、evidence-exit-code、evidence-file、evidence-sha256，evidence-file 使用 specDir 相对路径且 SHA-256 必须匹配；traceability.json 的 evidence 也应使用 specDir 相对路径；报告正文避免出现 TBD/TODO/FIXME/XXX、FAIL/失败/不通过 等会被机械扫描误判的字面量，历史初审阻断或异常路径需用中性表述。
2026-07-30 | 状态 | Walker 可扩展集成层已落地：新增 Provider Catalog/DriverRegistry 健康检测、walker doctor/providers/init CLI、受保护 /api/v1、EventBus + /api/v1/events/stream WebSocket、PlatformDriver + Feishu adapter；验证证据位于 specs/2026-07-30-walker-integration-layer，npm run check 1321 项测试全部通过。
2026-07-30 | 踩坑 | npm pack lifecycle 环境下，OpencodeTuiBridge 测试中等待业务 unref() timer 的 assert.rejects 可能触发 Node test runner 的 cancelledByParent。测试应使用带 ref 的 watchdog timer（assertRejectsWithRefTimer）维持事件循环，并避免 30/50ms 过短真实定时器；发布门禁全量测试使用 --test-concurrency=1 降低资源竞争。
2026-07-30 | 决策 | Walker daemon 后台运行态文件（walker.pid、walker.out.log、walker.err.log）应写入 Walker 数据目录：优先 WALKER_DATA_DIR（包括 .env 中配置），否则默认 ~/.walker；daemon 在计算 DATA_DIR/PID/LOG 常量前加载 .env。
2026-07-30 | 决策 | 日志体积优化已落地：内置 rotateLogFile 同步轮转，默认 10MB 阈值、每类保留 5 个归档；结构化 walker.log 默认关闭，仅 WALKER_LOG_FILE=true 时写入；daemon stdout/stderr 日志在启动前轮转；Admin 日志清空只处理 allowlist 当前日志和数字归档，并支持显式 cwd fallback。
2026-07-27 | 踩坑 | Loom 0.1.0 当前会检查 stage_history 中所有 approved 记录的 approval_fingerprints；若审批后 plan/tasks 变更并重新审批，旧 approved 记录可能继续触发 stale approval。处理本次 specs/2026-07-27-feishu-model-context-footer 时，在用户明确授权后仅将旧 approved 历史项标记为 superseded_by_reapproval，保留新审批指纹。
2026-07-27 | 踩坑 | Loom verification-artifacts validator 要求 test-report.md/verify-report.md 中 PASS evidence receipt 使用行级字段 evidence-command、evidence-exit-code、evidence-file、evidence-sha256；traceability.json 的 evidence 路径必须相对 specDir。报告中出现 todo 0 会被 placeholder scanner 误判，应避免在验证报告统计中保留该行。

## 🏗 架构决策（ADR）

_No ADRs yet._

## ⚠️ 踩坑记录

- **2026-07-31**: Loom verification/executing validators 对报告文本和证据格式较严格：test-report.md/verify-report.md 的 PASS evidence receipt 需要行级字段 evidence-command、evidence-exit-code、evidence-file、evidence-sha256，evidence-file 使用 specDir 相对路径且 SHA-256 必须匹配；traceability.json 的 evidence 也应使用 specDir 相对路径；报告正文避免出现 TBD/TODO/FIXME/XXX、FAIL/失败/不通过 等会被机械扫描误判的字面量，历史初审阻断或异常路径需用中性表述。
- **2026-07-30**: npm pack lifecycle 环境下，OpencodeTuiBridge 测试中等待业务 unref() timer 的 assert.rejects 可能触发 Node test runner 的 cancelledByParent。测试应使用带 ref 的 watchdog timer（assertRejectsWithRefTimer）维持事件循环，并避免 30/50ms 过短真实定时器；发布门禁全量测试使用 --test-concurrency=1 降低资源竞争。
- **2026-07-27**: Loom 0.1.0 当前会检查 stage_history 中所有 approved 记录的 approval_fingerprints；若审批后 plan/tasks 变更并重新审批，旧 approved 记录可能继续触发 stale approval。处理本次 specs/2026-07-27-feishu-model-context-footer 时，在用户明确授权后仅将旧 approved 历史项标记为 superseded_by_reapproval，保留新审批指纹。
- **2026-07-27**: Loom verification-artifacts validator 要求 test-report.md/verify-report.md 中 PASS evidence receipt 使用行级字段 evidence-command、evidence-exit-code、evidence-file、evidence-sha256；traceability.json 的 evidence 路径必须相对 specDir。报告中出现 todo 0 会被 placeholder scanner 误判，应避免在验证报告统计中保留该行。
- **2026-07-23**: 在 git worktree H:\\walker\\.worktree\\2026-07-23-admin-refactor 中运行 loom index --check 时，CodeGraph 报告索引属于主工作区 H:\\walker 而非当前 worktree；此时不能用该索引判断 worktree 未提交改动的新鲜调用关系或符号状态。不要自动运行 codegraph init -i，除非用户明确同意为 worktree 建本地索引；应以当前源码、git diff 和测试结果为准，并在报告中标明图索引跳过。
- **2026-07-20**: 飞书原生 question 卡片不要混用 legacy action/actions 与按钮 behaviors/form_submit；会导致发送 API 400。
- **2026-07-20**: 飞书原生 question 卡片的 select_static/multi_select_static 放在 tag:'form' 容器中会导致客户端看不到选项；11:17 现场验证 tag:'action' + actions 可显示选项。
- **2026-07-20**: 飞书原生 question 卡片的 select_static/multi_select_static 选项不要把说明拼进 text.content 换行，需用 option.description，并为 select 控件提供 placeholder，否则飞书端可能丢失选项渲染。
- **2026-07-20**: 飞书原生 question 卡片若把 select/input 与提交按钮放在普通 action 容器内，card.action.trigger 会到达但 form_value 为空；需使用 tag:form 容器并给提交按钮加 form_submit 行为。
- **2026-07-20**: node:test 父套件层 Promise resolution is still pending 会被标记 cancelledByParent 导致子测试显示失败；独立运行同一文件则 0 fail。诊断时须先单独运行该测试文件确认子测试本身是否通过。
- **2026-07-17**: 飞书卡片 multi_select_static 和 input 组件类型在代码库中首次使用，需实际卡片预览工具验证渲染效果；buildQuestionCard options.value 非空校验由 dispatcher _cmdAnswer required 校验兜底，卡片层未单独校验
- **2026-07-16**: loom-verification-before-completion 的 verify-artifacts.mjs 在当前环境缺失 C:\Users\tianxiqin\.config\opencode\src\core\artifact-checker.js，会报 ERR_MODULE_NOT_FOUND；本次以手工核验、npm run check 日志和 SHA-256 证据补足，项目代码本身验证通过。
- **2026-07-15**: TUI bridge clear 汇合状态机：_tryCompleteClear 中不能检查 runtime.currentSessionId !== oldSessionId，因为 clear 期间普通 register 合法更新 currentSessionId 后会导致 clear 误判失败。正确做法是仅依靠 controlDeliveryId 绑定 + control result 的 sessionId 一致性检查来保障两阶段提交正确性。另外 _preflightClear 中 _callFeishu 必须 await，否则 fire-and-forget 的 Promise reject 会产生未处理 rejection。
- **2026-07-15**: OpenCode TUI bridge 在 /new 后即使 promptAsync 成功且 Walker 日志显示 driver prompt completed eventCount>0，也不代表消息在当前 TUI route 可见。插件必须跟踪根 session.created/tui.session.select，并在投递目标 session 与 api.route.current 不一致时先调用 api.route.navigate('session', { sessionID })，再调用 promptAsync。
- **2026-07-15**: OpenCode 1.17.20 执行 /new 会通过 session.created 提供新根会话 ID；TUI bridge 插件不能只轮询可能滞后的 api.route.current。应维护事件驱动的 activeSessionId，监听 session.created 与 tui.session.select，并过滤带 parentID 的内部子会话。

## 👤 用户偏好

- permission 与 question 路由隔离：data.type=question 走 /answer 命令和 handleQuestion/handleQuestionReplied，传统 permission 走 /permit allow\|deny 和 handle/handleReplied；patch 卡片策略区分 transport（HTTP 直接 patch，TUI bridge 等 permission_replied 事件驱动）
