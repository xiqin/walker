---
owns: [src/platform/feishu/commands.js, test/feishu-commands.test.js, README.md, walker-console-v2.html]
reads: [specs/2026-07-28-new-cwd/spec.md]
depends_on: [T1]
requirements: [REQ-003]
behavior_ids: [REQ-003-B01, REQ-003-B02]
complexity: low
---

# Task 2: 帮助与文档更新

## 描述

更新 `/new` 的帮助 usage、README 命令表和管理端调试命令说明，使用户能发现 `[--cwd <path>]` 语法。

## 依赖

- 前置 task：T1
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/platform/feishu/commands.js` | 将 `/new` usage 更新为 `/new [agent] [title] [--cwd <path>]`。 |
| 修改 | `test/feishu-commands.test.js` | 增加 usage、help 和 token 传递测试。 |
| 修改 | `README.md` | 更新命令表中的 `/new` 语法。 |
| 修改 | `walker-console-v2.html` | 更新调试页面命令示例。 |

## 复杂度

low，原因：只涉及帮助文本、静态文档和轻量命令解析断言，不改变通用 parser。

## 实现步骤

- [ ] 步骤 1：更新 `COMMANDS.new.usage`，保留 `[agent] [title]` 顺序并追加 `[--cwd <path>]`。
- [ ] 步骤 2：在 `test/feishu-commands.test.js` 增加 `parseCommand('/new --cwd H:\project')` token 传递断言。
- [ ] 步骤 3：在 `test/feishu-commands.test.js` 增加 `COMMANDS.new.usage` 和 `formatHelp()` 包含 `--cwd <path>` 的断言。
- [ ] 步骤 4：更新 `README.md` 命令表中的 `/new` 语法与说明。
- [ ] 步骤 5：更新 `walker-console-v2.html` 的调试命令说明。

## 验收映射

| Requirement ID | Behavior ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | ----------- | -------- | -------- | -------- |
| REQ-003 | REQ-003-B01 | `COMMANDS.new.usage` 包含 `--cwd <path>` | `src/platform/feishu/commands.js:COMMANDS` | `test/feishu-commands.test.js` usage/help 断言 |
| REQ-003 | REQ-003-B02 | README 与调试页仍以 `/new [agent] [title]` 为基础并追加 `[--cwd <path>]` | `README.md`、`walker-console-v2.html` | 人工检查文档 diff |

## Traceability 更新要求

- planning 阶段：把本 task 的 `requirements` 和 `behavior_ids` 写入 `traceability.json` 的 `tasks` 字段。
- executing 阶段：补齐对应 behavior 的 `tests` 和 `evidence` 引用。
- 不允许只映射到 REQ 而遗漏 behavior 级映射。

## TDD 步骤

- [ ] 写失败测试：`test/feishu-commands.test.js` — usage/help 包含 `--cwd <path>`，parser 保持 token 传递。
- [ ] 运行确认失败：`node --test test/feishu-commands.test.js`
- [ ] 实现通过测试
- [ ] 重构（如需要）

## 测试说明

- 测试文件：`test/feishu-commands.test.js`
- 覆盖场景：帮助可发现性、通用 parser 兼容性、文档人工检查
- 验证命令：`node --test test/feishu-commands.test.js`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
