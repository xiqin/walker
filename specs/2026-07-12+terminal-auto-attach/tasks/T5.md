---
owns:
  - src/platform/feishu/cards.js
  - test/feishu-cards.test.js
reads:
  - src/core/session-service.js
depends_on: [T4]
requirements: [REQ-006]
complexity: low
---

# Task 5: 飞书卡片"设为焦点"按钮

## 描述

在 `/list` 命令返回的飞书会话列表卡片中，为非焦点 session 添加"设为焦点"按钮，点击后触发 `/use <session_id>` 命令切换焦点。

## 依赖

- 前置 task：T4（`/use` 已改为切焦点语义）
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/platform/feishu/cards.js` | `renderSessionListCard` 为非焦点 session 添加"设为焦点"按钮 |
| 修改 | `test/feishu-cards.test.js` | 新增"设为焦点"按钮测试 |

## 复杂度

low，原因：纯 UI 渲染改动，复用现有 `buildButtonValue` 机制，无后端逻辑变更。

## 实现步骤

- [ ] 步骤 1：在 `renderSessionListCard(sessions, currentSessionId, routeKey)` 的 action 按钮区域，为非焦点 session（`!isCurrent`）添加"设为焦点"按钮，value 使用 `buildButtonValue('cmd:/use', s.id, routeKey)`。焦点 session 显示"已聚焦"（禁用态）。
- [ ] 步骤 2：保持原有"绑定"按钮的语义变化——由于 `/use` 已从"绑定"改为"切焦点"（T4），将原"绑定/已绑定"按钮文案改为"设为焦点/已聚焦"，避免语义混淆。
- [ ] 步骤 3：在测试中验证非焦点 session 有"设为焦点"按钮、焦点 session 有"已聚焦"标记。

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-006 | /list 卡片有"设为焦点"按钮 | `src/platform/feishu/cards.js:renderSessionListCard` | `test/feishu-cards.test.js: 非焦点 session 有设为焦点按钮` |
| REQ-006 | 焦点 session 标记为"已聚焦" | `src/platform/feishu/cards.js:renderSessionListCard` | `test/feishu-cards.test.js: 焦点 session 标记已聚焦` |

## TDD 步骤

- [ ] 写失败测试：`test/feishu-cards.test.js` — 验证非焦点 session 有"设为焦点"按钮、焦点 session 有"已聚焦"标记
- [ ] 运行确认失败：`node --test test/feishu-cards.test.js`
- [ ] 实现通过测试：修改 cards.js 的 renderSessionListCard
- [ ] 重构（如需要）：确保现有 feishu-cards.test.js 测试全部通过

## 测试说明

- 测试文件：`test/feishu-cards.test.js`
- 覆盖场景：
  - 成功路径：非焦点 session 的按钮区域包含"设为焦点"按钮且 value 携带 `cmd:/use <id>`
  - 焦点标记：焦点 session 按钮文案为"已聚焦"、type 为 default
  - 多 session 路径：2+ session 列表中每个非焦点 session 都有按钮
- 验证命令：`node --test test/feishu-cards.test.js`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
- [ ] 现有 feishu-cards.test.js 测试全部通过（向后兼容）
