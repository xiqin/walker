---
owns:
  - src/config/env.js
  - src/admin/config.js
  - test/config-env.test.js
reads: []
depends_on: []
requirements: [REQ-007]
complexity: low
---

# Task 3: 配置项新增与白名单注册

## 描述

在 `src/config/env.js` 的 `loadEnvConfig` 中新增 4 个 OpenCode hook 相关配置项，并加入 `src/admin/config.js` 的 `EDITABLE_ENV_KEYS` 白名单，使其可通过 admin 界面编辑。

## 依赖

- 前置 task：无
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/config/env.js` | 在 `loadEnvConfig` 返回值中新增 4 个 hook 配置项 |
| 修改 | `src/admin/config.js` | 将 4 个环境变量名加入 `EDITABLE_ENV_KEYS` 数组 |
| 修改 | `test/config-env.test.js` | 新增配置项解析测试 |

## 复杂度

low，原因：纯配置添加，模式与现有配置项一致，无逻辑分支。

## 实现步骤

- [ ] 步骤 1：在 `src/config/env.js` 的 `loadEnvConfig` 返回对象中新增：
  - `walkerOpendcodeHookEnabled: parseBool(env.WALKER_OPENCODE_HOOK_ENABLED, true)`
  - `walkerOpendcodeHealthPollIntervalMs: parsePositiveInt(env.WALKER_OPENCODE_HEALTH_POLL_INTERVAL_MS, 5000)`
  - `walkerOpendcodeExitAction: env.WALKER_OPENCODE_EXIT_ACTION || 'cancel'`
  - `walkerOpendcodeNonFocusOutput: parseBool(env.WALKER_OPENCODE_NON_FOCUS_OUTPUT, true)`
- [ ] 步骤 2：在 `src/admin/config.js` 的 `EDITABLE_ENV_KEYS` 数组末尾添加：
  - `'WALKER_OPENCODE_HOOK_ENABLED'`
  - `'WALKER_OPENCODE_HEALTH_POLL_INTERVAL_MS'`
  - `'WALKER_OPENCODE_EXIT_ACTION'`
  - `'WALKER_OPENCODE_NON_FOCUS_OUTPUT'`
- [ ] 步骤 3：在 `test/config-env.test.js` 新增测试用例验证默认值和自定义值解析。

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-007 | WALKER_OPENCODE_HOOK_ENABLED 默认 true | `src/config/env.js:loadEnvConfig` | `test/config-env.test.js: hook enabled 默认 true` |
| REQ-007 | WALKER_OPENCODE_HEALTH_POLL_INTERVAL_MS 默认 5000 | `src/config/env.js:loadEnvConfig` | `test/config-env.test.js: health poll 默认 5000` |
| REQ-007 | WALKER_OPENCODE_EXIT_ACTION 默认 cancel | `src/config/env.js:loadEnvConfig` | `test/config-env.test.js: exit action 默认 cancel` |
| REQ-007 | WALKER_OPENCODE_NON_FOCUS_OUTPUT 默认 true | `src/config/env.js:loadEnvConfig` | `test/config-env.test.js: non focus output 默认 true` |
| REQ-007 | 4 个配置项加入 EDITABLE_ENV_KEYS | `src/admin/config.js:EDITABLE_ENV_KEYS` | `test/config-env.test.js: hook 配置项在可编辑白名单中` |

## TDD 步骤

- [ ] 写失败测试：`test/config-env.test.js` — 新增 4 个配置项的默认值和自定义值解析测试，以及白名单包含测试
- [ ] 运行确认失败：`node --test test/config-env.test.js`
- [ ] 实现通过测试：在 env.js 添加配置解析，在 config.js 添加白名单
- [ ] 重构（如需要）：无

## 测试说明

- 测试文件：`test/config-env.test.js`
- 覆盖场景：
  - 成功路径：4 个配置项使用默认值、4 个配置项使用自定义值
  - 白名单路径：4 个环境变量名在 `EDITABLE_ENV_KEYS` 中
- 验证命令：`node --test test/config-env.test.js`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
- [ ] 现有 config-env.test.js 测试全部通过（向后兼容）
