# Progress

> Auto-generated by loom. Do not edit manually.
> Last updated: 2026-07-13T03:50:48.351Z

## Pipeline

| Field | Value |
|-------|-------|
| Stage | `synced` |
| Type  | `feature` |
| Dynamic Steps | brainstorming → planning → approved → executing → verification → code-review-request → review-gate → code-review-response → synced |
| Started | 2026-07-12 01:07 |
| Updated | 2026-07-13 03:49 |

## Handoffs

| Stage/Task | Status | Summary | Artifacts |
|------------|--------|---------|-----------|
| `T1` | done | 将 SessionService 的 routes 数据结构从单值 { routeKey: sessionId } 升级为 { routeKey: { focusSessionId, sessions[], cwd, updatedAt } }。新增 _normalizeRoute/_readNormalized 内部迁移机制（检测 typeof routes[key] === 'string' 自动转换为对象格式并持久化）。新增 addSessionToRoute/setFocus/removeSessionFromRoute/listSessionsInRoute/getRouteCwd/setRouteCwd 6 个公开方法。改造 createSession/getCurrent/bindRoute/unbindRoute/getRouteForSession/deleteSession/cleanOrphanRoutes 以适配新结构。createSession 带 route 时加入 sessions 列表并将新 session 设为焦点（保持现有'覆盖旧绑定'语义的向后兼容）。unbindRoute 改为只移除焦点 session，列表空则删 route 条目，否则自动切焦点到第一个。deleteSession 从所有 route 的 sessions 列表移除，若为焦点则自动切到下一个活跃 session。所有 32 个测试通过（16 个原有 + 16 个新增），构建检查通过，无占位符残留。 | src/core/session-service.js, test/session-service.test.js |
| `T2` | done | 实现 Hook plugin 安装器与 receiver 端点。plugin-template.js 导出 getPluginSource(walkerPort) 生成 plugin 文件内容字符串（监听 session.created、上报 { opencodeBaseUrl, sessionId, cwd } 到 http://127.0.0.1:<port>/opencode/hook/session-created，Walker 不可达时静默忽略，硬编码 loopback 地址，不含任何 token/敏感信息）。installer.js 导出 installHookPlugin({ opencodeConfigDir, walkerPort, enabled })：enabled=false 返回 { installed:false, reason:'disabled' }；目标文件已存在则返回 { installed:false, reason:'already_exists' }（不覆盖）；不存在则写入并返回 { installed:true, path }；未传 opencodeConfigDir 时使用 os.homedir()/.config/opencode/plugins/walker-hook.js。receiver.js 导出 createHookReceiverRoutes(ctx) 返回路由数组（{ method, pattern, handler } 格式，与 core-routes.js 一致）：POST /opencode/hook/session-created 校验 sessionId/cwd 非空（400）、loopback 校验（403，支持 127.0.0.1/::1/::ffff:127.0.0.1）、复用 isAuthenticated(req, ctx.config.admin) 鉴权（401，admin token 未配置时放行）；_autoEnrollSession 实现幂等纳入（按 agentRef.opencodeSessionId 去重）、cwd 匹配 routeKey（精确优先、子目录次之、多候选取 updatedAt 最大）、创建 Walker session（agent='opencode', cwd, agentRef={ opencodeSessionId, serverUrl: opencodeBaseUrl }）、addSessionToRoute 纳入 route；无匹配 routeKey 时创建游离 session 返回 { sessionId, routeKey:null }。全部 17 个新增测试通过（6 installer + 11 receiver），npm test 全套 547 测试通过，无回归，无占位符残留。 | src/opencode-hook/plugin-template.js, src/opencode-hook/installer.js, src/opencode-hook/receiver.js, test/opencode-hook-installer.test.js, test/opencode-hook-receiver.test.js |
| `T3` | done | 在 src/config/env.js 的 loadEnvConfig 返回对象中新增 4 个 OpenCode hook 相关配置项（walkerOpendcodeHookEnabled 默认 true、walkerOpendcodeHealthPollIntervalMs 默认 5000、walkerOpendcodeExitAction 默认 cancel、walkerOpendcodeNonFocusOutput 默认 true），复用现有 parseBool/parsePositiveInt 辅助函数；在 src/admin/config.js 的 EDITABLE_ENV_KEYS 数组末尾追加对应 4 个环境变量名，使其可通过 admin 界面编辑；在 test/config-env.test.js 新增 10 个测试用例覆盖默认值、自定义值、无效值回落及白名单包含。全部 15 个测试通过，向后兼容。 | src/config/env.js, src/admin/config.js, test/config-env.test.js |
| `T4` | done | 改造 MessageDispatcher 的 /use、/list、/status 命令适配 1:N session 路由，实现非焦点 session 的 SSE watch 事件主动回卡片到群里并带 [session: wks_N] 标识。在 bootstrap.js 中集成 hook receiver 路由注册和 plugin 安装调用。具体改动：1) _cmdUse 从 bindRoute 改为 setFocus（切焦点而非覆盖式绑定），/use off 改为 removeSessionFromRoute（移除焦点 session，保留其他 session）。2) _cmdList 从 listSessions() 改为 listSessionsInRoute(cmd.routeKey)（只列出当前 route 下的 session，焦点排在第一位并标记）。3) _cmdStatus 新增 _formatRouteStatus 方法显示 route 多 session 状态格式（Route/Active sessions/Focus/Other）。4) _handleWatchedSessionEvent 新增 _isFocusSession 判断，非焦点 session 输出加 [session: <id前8位>] 前缀；nonFocusOutput=false 时非焦点 session 静默不回群。5) 构造参数新增 nonFocusOutput 选项（默认 true）。6) bootstrap.js 在 start() 中调用 installHookPlugin({ opencodeConfigDir, walkerPort, enabled })，在 createAdminIfEnabled 中调用 createHookReceiverRoutes 并注入 hookReceiverRoutes 到 admin server 上下文。7) admin/index.js 的 createAdminServerFromContext 新增 hookReceiverRoutes 参数支持，合并到 allRoutes。8) _cmdCancel 无需改动（getCurrent 返回焦点 session，T1 已保证）。全部 557 测试通过（52 message-dispatcher + 9 bootstrap + 496 其他），无回归，无占位符残留。 | src/dispatch/message-dispatcher.js, test/message-dispatcher.test.js, src/app/bootstrap.js, src/admin/index.js |
| `T5` | done | 将 renderSessionListCard 中的原'绑定/已绑定'按钮改为'设为焦点/已聚焦'，适配 T4 将 /use 命令从'绑定'改为'切焦点'的语义变更。非焦点 session 显示'设为焦点'按钮（type: primary，value: buildButtonValue('cmd:/use', s.id, routeKey)），点击触发 /use 切换焦点；焦点 session 显示'已聚焦'标记（type: default，禁用态）。新增 3 个测试用例验证非焦点 session 有'设为焦点'按钮、焦点 session 有'已聚焦'标记、按钮携带 routeKey。全部 18 个 feishu-cards 测试通过，无回归，无占位符残留。 | src/platform/feishu/cards.js, test/feishu-cards.test.js |
| `T6` | done | 实现心跳轮询管理器 createHealthPoller(options)，返回 { start, stop, track, untrack, getTrackedSessions }。track(sessionId, agentRef) 为每个 session 启动独立 setInterval 定时器（unref 避免阻止进程退出），定期 GET agentRef.serverUrl + '/global/health'，连续 2 次失败判定 detached。_handleDetached 执行：exitAction=cancel 且有 running turn 时调 dispatcher._cancelTurn 取消 turn，调 sessionService.removeSessionFromRoute 从 route 移除（若为焦点自动切到下一个），调 dispatcher._stopSessionWatch 停止 SSE watch，untrack 停止心跳。exitAction=none 时只停止 watch 不取消 turn。无 running turn 时退出不报错。bootstrap.js 集成：createApp 中创建 healthPoller 实例（注入 sessionService/driverRegistry/dispatcher/pollIntervalMs/exitAction/httpClient=opencodeDriver.httpClient），onSessionEnrolled 回调中调 healthPoller.track，start() 中调 _restoreHealthPollers 恢复已有 hook 纳入 session 的心跳追踪，stop() 中调 healthPoller.stop()。全部 581 测试通过（10 新增 health-poller 测试 + 571 原有），无回归，无占位符残留。 | src/opencode-hook/health-poller.js, test/opencode-hook-health-poller.test.js, src/app/bootstrap.js |
| `T7` | done | 更新 README.md 新增四个章节并更新配置和命令说明。1) 新增 'OpenCode 自动纳入' 章节：说明 hook plugin 机制（Walker 启动自动写入 ~/.config/opencode/plugins/walker-hook.js，不覆盖已存在文件）、session.created 事件上报流程、loopback + admin token 安全约束、WALKER_OPENCODE_HOOK_ENABLED=false 退回手动模式。2) 新增 '1:N Session 路由' 章节：说明 route 结构升级为 { focusSessionId, sessions[], cwd, updatedAt }、焦点 session 接收普通消息、非焦点 session 输出回群带 [session: wks_N] 标识、WALKER_OPENCODE_NON_FOCUS_OUTPUT=false 静默、/use 切焦点 + /list 卡片按钮切焦点。3) 新增 'OpenCode 退出行为' 章节：说明心跳轮询 /global/health 连续 2 次失败判定 detached、WALKER_OPENCODE_EXIT_ACTION=cancel 取消 turn 并移除/none 只记录、焦点自动切换、不 stop/delete session。4) 环境变量表格新增 4 项：WALKER_OPENCODE_HOOK_ENABLED、WALKER_OPENCODE_HEALTH_POLL_INTERVAL_MS、WALKER_OPENCODE_EXIT_ACTION、WALKER_OPENCODE_NON_FOCUS_OUTPUT，.env 示例块同步新增。5) 命令清单更新：/use 从'绑定'改为'切焦点'、/use off 从'清除绑定'改为'移除焦点 session 保留其他'、/list 从'查看所有 session'改为'列出当前 route 下所有 session 含设为焦点按钮'。6) Architecture 章节新增 1:N session 路由、hook plugin、心跳轮询三条能力描述。所有内容与 spec.md 验收标准和 T1-T6 实现代码一致，无占位符残留。 | README.md |
| `brainstorming` | done | 方案已确认：Hook Plugin + 1:N Session 路由。spec.md 已用新方案覆盖旧 wrapper 方案。核心决策：1) Walker 启动时自动安装 hook plugin；2) plugin 上报 session.created + cwd，Walker 按 cwd 找 routeKey 创建 session 加入 1:N 列表；3) routes 结构升级为 {focusSessionId, sessions[], cwd}，getCurrent 返回焦点；4) 非焦点 session 输出回群带标识；5) 每 session 独立心跳检测 detached，取消 turn 并从 route 移除，自动切焦点；6) /use 改切焦点，/list 加"设为焦点"按钮。session 和 driver 不改。10 个 REQ，19 条验收标准。 | specs/2026-07-12+terminal-auto-attach/spec.md, specs/2026-07-12+terminal-auto-attach/opencode-auto-attach-solution.md |
| `planning` | done | 规划阶段完成：7 个 task 拆分，覆盖 REQ-001 到 REQ-010 全部 10 个需求。validate-plan.mjs 校验通过。 | specs/2026-07-12+terminal-auto-attach/plan.md, specs/2026-07-12+terminal-auto-attach/tasks/T1.md, specs/2026-07-12+terminal-auto-attach/tasks/T2.md, specs/2026-07-12+terminal-auto-attach/tasks/T3.md, specs/2026-07-12+terminal-auto-attach/tasks/T4.md, specs/2026-07-12+terminal-auto-attach/tasks/T5.md, specs/2026-07-12+terminal-auto-attach/tasks/T6.md, specs/2026-07-12+terminal-auto-attach/tasks/T7.md |
| `executing` | done | 7 个 task 全部完成并通过 reviewer 审查。test-reporter verdict: PASS，587 个测试全部通过，0 失败。新增集成测试 test/integration-hook-routing.test.js（6 个用例）覆盖 hook 纳入→路由→派发、1:N 切焦点、非焦点输出、退出检测、E2E 路由可达 5 个跨模块场景。REQ-001 到 REQ-010 全部 PASS。关键修复：T1 修复了 admin 模块兼容 routes 新格式（route-admin.js/session-admin.js/admin-core-api.test.js）；T4 修复了非焦点 watch 启动机制（ensureWatchForSession + onSessionEnrolled 回调）和 receiver config 鉴权绕过，以及 hook 路由可达性（isAdminApiPath 扩展 /opencode/hook/ 前缀）。已知警告：health-poller 使用 /global/health 但 opencode-driver 使用 /health，需确认 OpenCode 实际端点；配置项命名 walkerOpendcode* 有拼写不一致（Opendcode vs Opencode）。 | src/core/session-service.js, src/opencode-hook/installer.js, src/opencode-hook/receiver.js, src/opencode-hook/plugin-template.js, src/opencode-hook/health-poller.js, src/config/env.js, src/admin/config.js, src/admin/route-admin.js, src/admin/session-admin.js, src/admin/router.js, src/admin/index.js, src/dispatch/message-dispatcher.js, src/app/bootstrap.js, src/platform/feishu/cards.js, README.md, test/session-service.test.js, test/config-env.test.js, test/opencode-hook-installer.test.js, test/opencode-hook-receiver.test.js, test/opencode-hook-health-poller.test.js, test/message-dispatcher.test.js, test/feishu-cards.test.js, test/admin-core-api.test.js, test/admin-server.test.js, test/integration-hook-routing.test.js, specs/2026-07-12+terminal-auto-attach/test-report.md |
| `verification` | done | 验证通过：587/587 PASS，REQ-001~010 全部 PASS，0 占位符，12 跨 task 接口一致，19/19 验收标准覆盖。verdict: PASS，evidence-sha256: 10C9CA22AA7C386995E7D6A5B76B207AB0C35F4EE7B75DC086D45695648DCAAA，evidence-exit-code: 0。 | specs/2026-07-12+terminal-auto-attach/verify-report.md |
| `code-review-request` | done | 审查请求已生成：18 文件变更（1173+ / 66-），7 个主要变更，587/587 测试通过，Standards 轴 5 警告 0 阻断，Spec 轴 0 偏差。 | specs/2026-07-12+terminal-auto-attach/review-request.md |
| `code-review-response` | done | 代码审查回复完成。无外部审查者反馈，自审查 5 个非阻断警告已逐一处置：W1 health 端点推迟到运行时确认（符合官方文档）、W2 配置项命名拒绝修改（spec 显式指定）、W3 marker 文案推迟到后续优化、W4 isSpaFallbackCandidate 拒绝修改（无实际影响 YAGNI）、W5 _readNormalized 性能推迟到后续优化。0 阻断问题，587 测试全部通过，审查通过。 | specs/2026-07-12+terminal-auto-attach/review-response.md |
| `synced` | done | 图后端同步与记忆更新完成。codegraph 索引已是最新（80 文件，801 节点，4088 边，DB 4.41MB），通过文件 watcher 自动同步，无需手动 loom index。新增 3 条结构化记忆：2 条决策（Hook Plugin 方案选型、1:N Session 路由升级）+ 1 条踩坑（isAdminApiPath 路由前缀匹配遗漏）。MEMORY.md 已导出。README.md 已由 T7 更新，无需额外入口文件改动。 | .loom/memory/store.json, .loom/memory/MEMORY.md |
