---
owns: [src/core/session-service.js, test/session-service.test.js]
reads: []
depends_on: []
requirements: [REQ-006, REQ-007]
complexity: low
---

# Task 1: Session 模型原子创建能力

## 描述

扩展 `SessionService.createSession`，允许创建 session 时复制并保存规范化模型对象。

## 依赖

- 前置 task：无
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/core/session-service.js` | 创建参数增加 `model` 并复制保存 |
| 修改 | `test/session-service.test.js` | 验证创建时保存和对象隔离 |

## 复杂度

low，原因：局部数据模型扩展，行为边界明确。

## 实现步骤

- [ ] 为模型创建行为添加失败测试
- [ ] 扩展 `createSession` 参数与 session 字段
- [ ] 验证模型对象被复制且未传模型时保持现有数据形态

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-006 | 新 session 创建时可直接保存模型对象 | `src/core/session-service.js:SessionService.createSession` | `createSession 保存并复制 model` |
| REQ-007 | session service 回归通过 | `test/session-service.test.js` | 模型创建测试及原有测试 |

## TDD 步骤

- [ ] 写失败测试：`test/session-service.test.js`，创建时保存并复制模型
- [ ] 运行确认失败：`node --test test/session-service.test.js`
- [ ] 实现通过测试
- [ ] 重构（如需要）

## 测试说明

- 测试文件：`test/session-service.test.js`
- 覆盖场景：模型保存、对象隔离、无模型兼容
- 验证命令：`node --test test/session-service.test.js`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
