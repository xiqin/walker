---
owns:
  - test/claude-tool-parity.integration.test.js
reads:
  - src/drivers/agent-driver.js
  - src/drivers/claude-driver.js
  - src/drivers/claude-transcript.js
  - src/drivers/claude-pty-broker.js
  - src/drivers/claude-bridge-sidecar.js
  - src/drivers/claude-attach-server.js
  - src/drivers/opencode-driver.js
  - src/config/env.js
  - src/app/bootstrap.js
  - src/providers/provider-catalog.js
  - test/claude-tui-reconnect.integration.test.js
  - test/claude-attach-list.test.js
depends_on: [T1, T2, T3]
requirements: [REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, REQ-006, REQ-007, REQ-008]
behavior_ids: [REQ-001-B03, REQ-001-B04, REQ-002-B05, REQ-003-B05, REQ-003-B06, REQ-004-B06, REQ-005-B04, REQ-005-B05, REQ-005-B06, REQ-006-B05, REQ-007-B01, REQ-007-B02, REQ-007-B03, REQ-007-B04, REQ-007-B05, REQ-008-B05, REQ-008-B06]
complexity: high
---

# Task 4: 跨链路回归与安全验收

## 描述

创建不侵占生产文件的集成测试，验证统一配置经过 bootstrap 到长期 TUI、sidecar 重连和 transcript 事件链路时仍满足单进程、安全和兼容约束。

## 依赖

- 前置 task：T1、T2、T3
- 外部依赖：完成后的 Claude driver、broker、transcript 和配置契约

## 涉及文件

| 操作 | 文件路径 | 说明 |
| ---- | -------- | ---- |
| 创建 | `test/claude-tool-parity.integration.test.js` | 跨配置、runtime、事件、能力与安全的集成回归 |

## 复杂度

high，原因：需要验证多个模块之间的真实契约和禁止副作用，而不通过修改生产代码掩盖缺陷。

## 实现步骤

- [ ] 构造记录 spawn、PTY 写入、sidecar reuse 和 transcript 输出的可控集成 fixture。
- [ ] 验证完整配置 create/resume 公共参数一致，多轮 prompt 不增加 spawn，sidecar 可用时不 resume。
- [ ] 验证 permission/question unsupported 不写 PTY，权限 UI/lease 场景不注入选择按键。
- [ ] 验证 transcript 事件、历史列表和 reconnect 在同一配置下工作，attach/detach/stop 清理不变。
- [ ] 对日志、AgentEvent、runtime state 注入 attach token、API key、authorization 和配置 secret 哨兵并扫描。
- [ ] 验证 catalog 对 Claude 的降级说明以及 OpenCode 行为、协议和专属源码不变。
- [ ] 运行 Claude 定向套件、OpenCode 定向套件和最终 `npm test`。

## 验收映射

| Requirement | Behaviors | 验收重点 | 测试文件 |
| ----------- | --------- | -------- | -------- |
| REQ-001 | B03-B04 | 单进程和默认兼容 | `test/claude-tool-parity.integration.test.js` |
| REQ-002 | B05 | 跨链路无敏感配置泄漏 | 同上 |
| REQ-003 | B05-B06 | 非同构声明与权限诊断 | 同上 |
| REQ-004 | B06 | 纯文本与原回复边界兼容 | 同上 |
| REQ-005 | B04-B06 | 禁止 ANSI 自动应答、能力降级与事件脱敏 | 同上 |
| REQ-006 | B05 | 旧最小部署兼容 | 同上 |
| REQ-007 | B01-B05 | 历史列表、重连、lease、生命周期、OpenCode 零回归 | 同上及既有套件 |
| REQ-008 | B05-B06 | 全通路 secret 扫描和禁止静默降权 | 同上 |

## Traceability 更新要求

- 将集成测试名追加到对应 behavior 的 `tests`，并记录命令、退出码和日志摘要 evidence。
- T4 不替代 T1-T3 的单元证据，只提供跨模块补充证据。

## TDD 步骤

- [ ] 写失败测试：`test/claude-tool-parity.integration.test.js`
- [ ] 运行确认失败：`node --test test/claude-tool-parity.integration.test.js`
- [ ] 仅在 T1-T3 回流任务中修复发现的生产缺陷，T4 自身只维护集成 fixture 和断言
- [ ] 运行 Claude、OpenCode 定向回归与 `npm test`

## 完成标准

- [ ] 集成测试覆盖所有列出的禁止副作用和恢复路径
- [ ] Claude 全链路、OpenCode 回归和 `npm test` 全绿
- [ ] `git diff --check` 通过
- [ ] 工作区原有未提交改动未被回退或覆盖
