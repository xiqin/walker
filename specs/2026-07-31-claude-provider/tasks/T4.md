---
owns: [specs/2026-07-31-claude-provider/test-report.md]
reads: [specs/2026-07-31-claude-provider/spec.md, specs/2026-07-31-claude-provider/requirements.json, specs/2026-07-31-claude-provider/traceability.json, package.json]
depends_on: [T1, T2, T3]
requirements: [REQ-001, REQ-008, REQ-009]
behavior_ids: [REQ-001-B04, REQ-008-B03, REQ-009-B02, REQ-009-B03]
complexity: medium
---

# Task 4: 资料基线、回归验证与执行报告

## 目标

运行整体验证并生成 `test-report.md`。报告必须记录 Claude Code 当前资料基线、测试命令、结果、失败原因或通过证据，并确认自动测试未发送真实 Claude prompt。

## 依赖

依赖 T1、T2、T3 的实现和局部测试完成。

## Affected Files

- `specs/2026-07-31-claude-provider/test-report.md`

## 实现步骤

## Checklist

- [ ] 重新记录可用资料基线：本机 `claude --version`、`claude --help` 的关键参数、`npm view @anthropic-ai/claude-code` latest/stable/bin 信息；如官方 docs 仍不可抓取，记录失败原因。
- [ ] 运行 T1-T3 的定向测试和 `npm test`。
- [ ] 检查测试文件和实现，确认不存在真实 Claude prompt 调用路径，fake CLI 注入覆盖自动测试。
- [ ] 写入 `specs/2026-07-31-claude-provider/test-report.md`，包含每个 REQ 的测试证据入口，供 executing 阶段更新 `traceability.json` 的 tests/evidence。

## Acceptance Mapping

覆盖 `REQ-001`、`REQ-008`、`REQ-009` 中与回归验证、资料新鲜度证据、测试报告和禁止真实 Claude prompt 相关的 behavior。

## 测试步骤

1. 执行 `npm test`。
2. 若 `npm test` 失败，记录失败命令、失败摘要和修复结果；不得把失败状态标记为完成。
3. 扫描新增/修改测试，确认未调用真实 `claude --print`。

## 完成标准

`test-report.md` 完整记录资料基线、验证命令和结果，且 OpenCode 回归与 Claude fake CLI 测试通过。
