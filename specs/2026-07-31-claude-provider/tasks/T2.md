---
owns: [src/app/bootstrap.js, src/providers/provider-catalog.js, src/providers/provider-health.js, test/bootstrap.test.js, test/provider-catalog.test.js, test/driver-registry.test.js, test/providers-cli.test.js, test/doctor-cli.test.js]
reads: [src/drivers/stub-drivers.js, src/drivers/driver-registry.js, specs/2026-07-31-claude-provider/spec.md]
depends_on: [T1]
requirements: [REQ-001, REQ-002, REQ-008]
behavior_ids: [REQ-001-B01, REQ-001-B02, REQ-001-B03, REQ-001-B04, REQ-002-B03, REQ-008-B04]
complexity: medium
---

# Task 2: Provider Catalog 与 Bootstrap 注册

## 目标

将真实 `ClaudeDriver` 接入应用启动、provider catalog 与 provider health/doctor 状态。`claude` 不再显示为 stub 未实现，同时不降低 `opencode` 的默认注册与健康逻辑。

## 依赖

依赖 T1 完成 `ClaudeDriver` 核心实现。

## Affected Files

- `src/app/bootstrap.js`
- `src/providers/provider-catalog.js`
- `src/providers/provider-health.js`
- `test/bootstrap.test.js`
- `test/provider-catalog.test.js`
- `test/driver-registry.test.js`
- `test/providers-cli.test.js`
- `test/doctor-cli.test.js`

## 实现步骤

## Checklist

- [ ] 修改 `src/app/bootstrap.js`，引入 `ClaudeDriver` 并注册到 registry，保留 `codex` stub。
- [ ] 传入 Claude driver 所需配置：命令、模型、fallback model、agent、permission mode、tools、add-dir、config dir、timeout 与可注入 runtime。
- [ ] 更新 `src/providers/provider-catalog.js` 中 Claude capabilities 与 configKeys，明确 `http:false`、不声明 OpenCode TUI bridge，补齐 `CLAUDE_*` 配置键。
- [ ] 视现有实现修改 `src/providers/provider-health.js`，使 Claude provider 的 doctor/version 状态能展示 registered、healthy、version、problems、suggestions，并避免触发真实 prompt。
- [ ] 保持 `DriverRegistry` 的 registration 附加语义不破坏现有调用；仅在必要时扩展字段。

## Acceptance Mapping

覆盖 `REQ-001`、`REQ-002`、`REQ-008` 中与真实 Claude 注册、provider 状态、OpenCode 不回归和 doctor/provider list 诊断相关的 behavior。

## 测试步骤

1. 更新 `test/bootstrap.test.js`，断言 bootstrap 后 `registry.get('claude')` 返回非 stub driver，`opencode` 仍注册。
2. 更新 `test/provider-catalog.test.js`，断言 Claude configKeys 与 capabilities 符合规格。
3. 更新 `test/driver-registry.test.js`、`test/providers-cli.test.js` 或 `test/doctor-cli.test.js`，覆盖 Claude provider 状态不再是 stub 未实现，并且 provider list/doctor 不调用 prompt。

## 完成标准

Claude provider 在 registry/provider/admin doctor 层可诊断，`opencode` 相关 provider/bootstrap 测试继续通过。
