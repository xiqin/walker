---
owns: [README.md, DESIGN.md, .env.example, specs/2026-07-09-feishu-opendray-bridge/implementation-notes.md]
reads: [specs/2026-07-09-feishu-opendray-bridge/spec.md, specs/2026-07-09-feishu-opendray-bridge/plan.md]
depends_on: [T7]
requirements: [REQ-001, REQ-026, REQ-027, REQ-028]
complexity: medium
---

# Task 8: README、配置示例与扩展文档

## 描述

更新用户可见文档和配置示例，删除旧 opendray 运行说明，补齐飞书、opencode、Windows/WSL、命令清单和未来 driver 扩展说明。

## 依赖

- 前置 task：T7
- 外部依赖：飞书开放平台、opencode 安装方式、Windows/WSL 环境

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `README.md` | 改为 Walker 飞书多 Agent CLI 桥接器说明 |
| 修改 | `DESIGN.md` | 与最终代码结构和执行方式保持一致 |
| 修改 | `.env.example` | 删除 `OPENDRAY_*`，补齐 Walker/opencode/runtime/飞书配置 |
| 创建 | `specs/2026-07-09-feishu-opendray-bridge/implementation-notes.md` | 记录 opencode API 实测结果、未验证环境和后续 driver 扩展边界 |

## 复杂度

medium，原因：文档范围较大，但基于已实现功能和 spec 做一致性更新。

## 实现步骤

- [ ] 步骤 1：README 首屏说明项目目标：不依赖 opendray，通过飞书操控本机 Walker Agent Hub。
- [ ] 步骤 2：README 写清安装依赖、飞书长连接配置、`.env` 配置、`npm start`、后台脚本和 `npm run check`。
- [ ] 步骤 3：README 写清命令清单：`/new`、`/list`、`/use`、`/use off`、`/current`、`/stop`、`/delete`、`/agents`、`/runtime`、`/help`。
- [ ] 步骤 4：README 写清 opencode 安装、`opencode serve` 自动启动开关、Windows 原生和 WSL 配置差异。
- [ ] 步骤 5：`.env.example` 删除所有 `OPENDRAY_*` 示例，补齐 `FEISHU_*`、`WALKER_*`、`OPENCODE_*`。
- [ ] 步骤 6：`implementation-notes.md` 记录 Claude Code 和 Codex driver 当前为扩展点，说明后续实现协议入口。
- [ ] 步骤 7：DESIGN 与最终模块路径、数据目录、driver 接口保持一致。

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-001 | 文档不再要求 opendray bridge、REST 或 token | `README.md`、`.env.example` | 文档审查 |
| REQ-026 | 文档说明 Claude Code driver 扩展点和 stream-json stdio 方向 | `implementation-notes.md` | 文档审查 |
| REQ-027 | 文档说明 Codex driver 扩展点和协议待接入边界 | `implementation-notes.md` | 文档审查 |
| REQ-028 | README 描述安装、飞书配置、Windows/WSL runtime、opencode server、命令清单 | `README.md` | 文档审查 |

## TDD 步骤

- [ ] 写失败检查：用人工审查清单核对 README 是否仍包含旧 opendray 运行路径。
- [ ] 运行确认失败：在文档更新前执行 `npm run check`，确认代码基线仍通过。
- [ ] 实现文档更新
- [ ] 重跑验证：`npm run check`
- [ ] 审查 `.env.example` 与 `src/config/env.js` 字段完全对应

## 测试说明

- 测试文件：无新增自动化测试；执行文档审查和配置字段一致性检查
- 覆盖场景：安装、配置、启动、命令、Windows/WSL、opencode、扩展 driver
- 验证命令：`npm run check`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
