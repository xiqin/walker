---
owns: [src/platform/feishu/commands.js, test/feishu-commands.test.js]
reads: [specs/2026-07-11+walker-short-term-evolution/spec.md]
depends_on: []
requirements: [REQ-002, REQ-003]
complexity: low
---

# Task 2: 命令注册与帮助文案

## 描述

注册 `/cancel`、`/status`、`/ps`，让飞书命令解析和帮助清单先具备入口。

## 依赖

- 前置 task：无
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/platform/feishu/commands.js` | 增加命令定义和帮助文案。 |
| 修改 | `test/feishu-commands.test.js` | 覆盖新增命令解析和命令表。 |

## 复杂度

low，原因：只涉及命令表和解析测试，不触碰调度执行逻辑。

## 实现步骤

- [ ] 步骤 1：在 `COMMANDS` 中新增 `cancel`，用法 `/cancel`，描述为取消当前 turn。
- [ ] 步骤 2：在 `COMMANDS` 中新增 `status`，用法 `/status`，描述为查看当前会话状态。
- [ ] 步骤 3：在 `COMMANDS` 中新增 `ps`，用法 `/ps`，描述为 `/status` 的别名。
- [ ] 步骤 4：更新命令解析测试，确保三个命令都返回 `type: 'command'`。
- [ ] 步骤 5：更新命令表完整性测试，确保帮助输出能包含新增命令。

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-002 | `/cancel` 可被识别为 Walker 命令。 | `src/platform/feishu/commands.js:COMMANDS` | `parseCommand /cancel` |
| REQ-003 | `/status` 和 `/ps` 可被识别为 Walker 命令。 | `src/platform/feishu/commands.js:COMMANDS` | `parseCommand /status`、`parseCommand /ps` |

## TDD 步骤

- [ ] 写失败测试：`test/feishu-commands.test.js` — `/cancel`、`/status`、`/ps` 解析。
- [ ] 运行确认失败：`node --test test/feishu-commands.test.js`
- [ ] 实现通过测试。
- [ ] 重构：不需要额外抽象。

## 测试说明

- 测试文件：`test/feishu-commands.test.js`
- 覆盖场景：命令解析、命令表包含新增命令。
- 验证命令：`node --test test/feishu-commands.test.js`

## 完成标准

- [ ] 所有步骤完成。
- [ ] 目标测试通过。
- [ ] 帮助文案简洁且与实际语义一致。
