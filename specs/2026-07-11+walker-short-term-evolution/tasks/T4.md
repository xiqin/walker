---
owns: [README.md]
reads: [specs/2026-07-11+walker-short-term-evolution/spec.md, specs/2026-07-11+walker-short-term-evolution/plan.md]
depends_on: [T1, T2, T3]
requirements: [REQ-006]
complexity: low
---

# Task 4: 文档与验收验证

## 描述

更新 README，使新增配置、命令和长任务行为与实现一致，并完成全量验证。

## 依赖

- 前置 task：T1、T2、T3
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `README.md` | 补充心跳配置、最大 turn 时长、`/cancel`、`/status`、`/ps` 和长任务行为。 |

## 复杂度

low，原因：只更新文档并执行已有验证命令。

## 实现步骤

- [ ] 步骤 1：在配置章节新增 `WALKER_PROMPT_HEARTBEAT_INITIAL_MS`、`WALKER_PROMPT_HEARTBEAT_INTERVAL_MS`、`WALKER_PROMPT_HEARTBEAT_STUCK_MS`、`WALKER_MAX_TURN_TIME_MINS`。
- [ ] 步骤 2：在命令章节新增 `/cancel`、`/status`、`/ps`。
- [ ] 步骤 3：在长任务或进度说明中写明心跳只更新原进度卡片，不发送普通群消息。
- [ ] 步骤 4：写明 `/cancel` 第一版对 OpenCode 复用 stop 能力，但 Walker session 保留并回到 idle。
- [ ] 步骤 5：运行文档关键字检查，确认 README 包含新增命令和配置项。
- [ ] 步骤 6：运行全量测试和 CodeGraph 同步。

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-006 | README 包含新增命令、配置项和长任务行为说明。 | `README.md` | README 关键字检查、`npm test` |

## TDD 步骤

- [ ] 写失败检查：用搜索确认 README 暂未包含新增配置和命令。
- [ ] 运行确认失败：`rg "WALKER_PROMPT_HEARTBEAT_INITIAL_MS|/cancel|/status|WALKER_MAX_TURN_TIME_MINS" README.md`
- [ ] 更新 README。
- [ ] 运行全量验证。
- [ ] 重构：不需要代码重构。

## 测试说明

- 测试文件：无新增代码测试；依赖 T1 到 T3 的自动化测试。
- 覆盖场景：README 与实际行为一致，全量测试无回归。
- 验证命令：`npm test`、`codegraph sync .`

## 完成标准

- [ ] README 包含四个新增环境变量。
- [ ] README 包含 `/cancel`、`/status`、`/ps`。
- [ ] README 说明最大执行时长和取消后的 session 保留语义。
- [ ] `npm test` 通过。
- [ ] `codegraph sync .` 完成。
