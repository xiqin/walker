---
owns: [src/drivers/opencode-driver.js, test/opencode-driver.test.js]
reads: [src/opencode-tui-bridge/bridge.js]
depends_on: [T2]
requirements: [REQ-011]
complexity: medium
---

# Task 3: OpencodeDriver.replyPermission 对 TUI bridge 转发

## 描述

修改 `OpencodeDriver.replyPermission`，当 `sessionRef.transport='tui-bridge'` 时转发到 `tuiBridge.replyQuestion(sessionRef, permissionId, response)`，不再抛 `'replyPermission is not supported for tui-bridge transport'`。HTTP transport 行为保持不变。

## 依赖

- 前置 task：T2（replyQuestion 方法必须存在）
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/drivers/opencode-driver.js` | `replyPermission` 第 527-529 行的 TUI bridge 抛错分支改为转发到 `tuiBridge.replyQuestion` |
| 修改 | `test/opencode-driver.test.js` | 新增 TUI bridge transport 下 replyPermission 转发测试 |

## 复杂度

medium，原因：需修改现有抛错分支为转发，并保证 HTTP transport 路径不受影响；需校验 tuiBridge 和 replyQuestion 方法存在性。

## 实现步骤

- [ ] 步骤 1：定位 `src/drivers/opencode-driver.js:527-529` 的 `_isTuiBridge` 分支。
- [ ] 步骤 2：将 `throw new Error('replyPermission is not supported for tui-bridge transport')` 替换为：
  ```js
  if (!this.tuiBridge || typeof this.tuiBridge.replyQuestion !== 'function') {
    throw new Error('OpencodeDriver replyPermission requires configured tuiBridge with replyQuestion');
  }
  return this.tuiBridge.replyQuestion(sessionRef, permissionId, response);
  ```
- [ ] 步骤 3：确认 HTTP transport 分支（第 530-543 行）不变，`response` 参数支持 string 或 string[]（POST body 直接传，opencode 上游消费）。
- [ ] 步骤 4：确认现有 `cancel`/`delete`/`clearSession` 等方法的 TUI bridge 分支模式一致（`_isTuiBridge` + 转发）。

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-011 | TUI bridge transport 下 replyPermission 转发到 tuiBridge.replyQuestion，不再抛错 | `src/drivers/opencode-driver.js:520` `replyPermission` | `test/opencode-driver.test.js` 新增 TUI bridge replyPermission 转发测试 |

## TDD 步骤

- [ ] 写失败测试：`test/opencode-driver.test.js` 新增测试：
  - `replyPermission tui-bridge transport 转发到 tuiBridge.replyQuestion`
  - `replyPermission tui-bridge transport 无 tuiBridge 时抛错`
  - `replyPermission tui-bridge transport tuiBridge 无 replyQuestion 方法时抛错`
  - `replyPermission http transport 保持现有行为（现有测试）`
- [ ] 运行确认失败：`node --test test/opencode-driver.test.js`
- [ ] 实现通过测试：修改 replyPermission 分支
- [ ] 重构（如需要）

## 测试说明

- 测试文件：`test/opencode-driver.test.js`
- 覆盖场景：TUI bridge 转发成功、tuiBridge 缺失、replyQuestion 方法缺失、HTTP transport 不受影响、response 为 string[] 时正确传递
- 验证命令：`npm run lint` / `node --test test/opencode-driver.test.js`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留（无占位符）
- [ ] 现有 `test/opencode-driver.test.js` 全部通过
