---
owns: [src/platform/feishu/commands.js, test/feishu-commands.test.js]
reads: []
depends_on: []
requirements: [REQ-003, REQ-004, REQ-005, REQ-006]
complexity: low
---

# Task 6: commands.js 新增 answer 条目

## 描述

在 `src/platform/feishu/commands.js` 的 `COMMANDS` 表中新增 `answer` 条目，使 `parseCommand` 能识别 `/answer` 命令并路由到 dispatcher 的 `_cmdAnswer` 处理。

## 依赖

- 前置 task：无
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/platform/feishu/commands.js` | COMMANDS 表新增 `answer` 条目 |
| 修改 | `test/feishu-commands.test.js` | 新增 answer 命令解析测试 |

## 复杂度

low，原因：仅在 COMMANDS 对象新增一个条目，parseCommand 自动识别。

## 实现步骤

- [ ] 步骤 1：在 `src/platform/feishu/commands.js:2-19` 的 `COMMANDS` 对象中，在 `permit` 条目后新增：
  ```js
  answer: { desc: '回复交互式问题', usage: '/answer <questionId> <value> | /answer <questionId> --form' },
  ```
- [ ] 步骤 2：确认 `COMMAND_LIST` 自动通过 `Object.entries(COMMANDS)` 包含新条目。
- [ ] 步骤 3：确认 `parseCommand` 识别 `/answer question_id dev` 为 `{ type: 'command', name: 'answer', args: ['question_id', 'dev'] }`。
- [ ] 步骤 4：确认 `parseCommand` 识别 `/answer question_id --form` 为 `{ type: 'command', name: 'answer', args: ['question_id', '--form'] }`。

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-003 | /answer 命令可被 parseCommand 识别 | `src/platform/feishu/commands.js:COMMANDS.answer` | `test/feishu-commands.test.js` answer 命令解析测试 |
| REQ-004 | /answer 命令支持 single_select value 参数 | `src/platform/feishu/commands.js:COMMANDS.answer` | `test/feishu-commands.test.js` answer 带 value 参数测试 |
| REQ-005 | /answer 命令支持 --form 表单提交 | `src/platform/feishu/commands.js:COMMANDS.answer` | `test/feishu-commands.test.js` answer 带 --form 测试 |
| REQ-006 | /answer 命令支持 text 输入提交 | `src/platform/feishu/commands.js:COMMANDS.answer` | `test/feishu-commands.test.js` answer text 场景测试 |

## TDD 步骤

- [ ] 写失败测试：`test/feishu-commands.test.js` 新增测试：
  - `parseCommand 识别 /answer <id> <value>`
  - `parseCommand 识别 /answer <id> --form`
  - `COMMANDS.answer 存在且有 desc 和 usage`
  - `formatHelp 包含 answer 命令`
- [ ] 运行确认失败：`node --test test/feishu-commands.test.js`
- [ ] 实现通过测试：新增 answer 条目
- [ ] 重构（如需要）

## 测试说明

- 测试文件：`test/feishu-commands.test.js`
- 覆盖场景：answer 命令解析、带 value 参数、带 --form 参数、COMMANDS 表包含条目、formatHelp 输出包含
- 验证命令：`npm run lint` / `node --test test/feishu-commands.test.js`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留（无占位符）
- [ ] 现有 `test/feishu-commands.test.js` 全部通过
