---
owns: [test/feishu-api.test.js, test/message-dispatcher.test.js]
reads: [specs/2026-07-27-feishu-model-context-footer/spec.md, specs/2026-07-27-feishu-model-context-footer/requirements.json]
depends_on: [T1, T2]
requirements: [REQ-001, REQ-002, REQ-003, REQ-004]
behavior_ids: [REQ-001-B01, REQ-001-B02, REQ-001-B03, REQ-001-B04, REQ-002-B01, REQ-002-B02, REQ-002-B03, REQ-002-B04, REQ-003-B01, REQ-003-B02, REQ-003-B03, REQ-003-B04, REQ-004-B01, REQ-004-B02, REQ-004-B03]
complexity: medium
---

# Task 3: 飞书页脚测试覆盖与回归验证

## 描述

补齐飞书页脚功能的单元测试和回归验证，确保所有 requirement 与 behavior 都有真实测试和证据。

## 依赖

- 前置 task：T1, T2
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `test/feishu-api.test.js` | 增加 FeishuApi payload 断言，覆盖文本、Markdown、卡片、幂等、分片和字段边界。 |
| 修改 | `test/message-dispatcher.test.js` | 增加 dispatcher metadata 传递、重试不变和无外部调用断言。 |

## 复杂度

medium，原因：测试需要解析飞书请求体中的二次 JSON 内容，并同时覆盖 API 层与 dispatcher 层行为。

## 实现步骤

- [ ] 为 `test/feishu-api.test.js` 增加解析 `_request` body 的辅助断言，读取 text content 和 interactive card content。
- [ ] 覆盖 `replyText`、`sendText`、`replyMarkdown`、`sendMarkdown`、`replyCard`、`patchCard` 的页脚存在性和正文保持不变。
- [ ] 覆盖已含页脚输入再次发送时页脚只出现一次。
- [ ] 覆盖模型对象、模型字符串、模型缺失、上下文 token 存在、上下文缺失和异常输入。
- [ ] 为 `test/message-dispatcher.test.js` 增加 `_callFeishu` 调用断言，确认 runtime metadata 传入支持方法且 retry 次数不变。
- [ ] 运行规划和相关测试命令，并在 executing 阶段把测试文件和命令输出补入 `traceability.json` 的 `tests` 与 `evidence`。

## 验收映射

| Requirement ID | Behavior ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | ----------- | -------- | -------- | -------- |
| REQ-001 | REQ-001-B01 | 相关发送路径生成的飞书内容均包含模型和上下文字段。 | `test/feishu-api.test.js` | `FeishuApi 页脚覆盖测试` |
| REQ-001 | REQ-001-B02 | 同一 payload 中运行信息页脚最多出现一次。 | `test/feishu-api.test.js` | `FeishuApi 页脚幂等测试` |
| REQ-001 | REQ-001-B03 | 正文前缀与原输入完全一致，页脚追加在正文之后。 | `test/feishu-api.test.js` | `FeishuApi 正文兼容测试` |
| REQ-001 | REQ-001-B04 | 页脚始终使用模型和上下文两个固定字段名。 | `test/feishu-api.test.js` | `FeishuApi 页脚可观测性测试` |
| REQ-002 | REQ-002-B01 | 对象模型显示为 provider/modelID；字符串模型按已有规则显示。 | `test/feishu-api.test.js` | `FeishuApi 模型解析测试` |
| REQ-002 | REQ-002-B02 | 缺失模型场景发送成功且模型字段为默认模型或 unknown。 | `test/feishu-api.test.js` | `FeishuApi 模型边界测试` |
| REQ-002 | REQ-002-B03 | 解析异常时页脚模型为 unknown，发送流程继续。 | `test/feishu-api.test.js` | `FeishuApi 模型恢复测试` |
| REQ-002 | REQ-002-B04 | 同一路由会话的模型更新后，后续发送 metadata 使用新的模型值。 | `test/message-dispatcher.test.js` | `MessageDispatcher 模型状态更新测试` |
| REQ-003 | REQ-003-B01 | 上下文字段包含数值和单位。 | `test/feishu-api.test.js` | `FeishuApi 上下文解析测试` |
| REQ-003 | REQ-003-B02 | 缺失上下文大小不会产生空字符串或 undefined。 | `test/feishu-api.test.js` | `FeishuApi 上下文边界测试` |
| REQ-003 | REQ-003-B03 | 解析异常时上下文字段为 unknown，发送流程继续。 | `test/feishu-api.test.js` | `FeishuApi 上下文恢复测试` |
| REQ-003 | REQ-003-B04 | 页脚上下文字段为数值加单位或 unknown。 | `test/feishu-api.test.js` | `FeishuApi 上下文可观测性测试` |
| REQ-004 | REQ-004-B01 | 长文本发送仍通过现有 splitTextChunks 逻辑分片。 | `test/feishu-api.test.js` | `FeishuApi 分片回归测试` |
| REQ-004 | REQ-004-B02 | 可重试方法仍按原次数重试，失败 fallback 行为不变。 | `test/message-dispatcher.test.js` | `MessageDispatcher 重试回归测试` |
| REQ-004 | REQ-004-B03 | 发送路径不额外调用 driver 或外部 API 获取模型/上下文。 | `test/message-dispatcher.test.js` | `MessageDispatcher 无外部调用测试` |

## Traceability 更新要求

- planning 阶段：把本 task 的 `requirements` 和 `behavior_ids` 写入 `traceability.json` 的 `tasks` 字段。
- executing 阶段：补齐对应 behavior 的 `tests` 和 `evidence` 引用。
- 不允许只映射到 REQ 而遗漏 behavior 级映射。

## TDD 步骤

- [ ] 写失败测试：`test/feishu-api.test.js` 和 `test/message-dispatcher.test.js` - 覆盖全部 behavior。
- [ ] 运行确认失败：`node --test test/feishu-api.test.js test/message-dispatcher.test.js`
- [ ] 实现通过测试
- [ ] 重构测试辅助方法并保持断言可读

## 测试说明

- 测试文件：`test/feishu-api.test.js`、`test/message-dispatcher.test.js`
- 覆盖场景：统一页脚、幂等、模型解析、上下文解析、格式兼容、重试可靠性、无外部调用
- 验证命令：`node --test test/feishu-api.test.js test/message-dispatcher.test.js`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
