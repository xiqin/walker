---
owns: [test/claude-tui-reconnect.integration.test.js, specs/2026-08-06-claude-tui-reconnect/test-report.md, specs/2026-08-06-claude-tui-reconnect/verify-report.md]
reads: [src/drivers/claude-bridge-sidecar.js, src/drivers/claude-driver.js, src/dispatch/message-dispatcher.js, src/cli/claude-attach-command.js, src/app/bootstrap.js, test/claude-bridge-sidecar.test.js, test/claude-driver.test.js, test/message-dispatcher.test.js, test/claude-attach-command.test.js, test/bootstrap.test.js]
depends_on: [T2, T3, T4, T5]
requirements: [REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, REQ-006]
behavior_ids: [REQ-001-B01, REQ-001-B02, REQ-001-B03, REQ-001-B04, REQ-001-B05, REQ-001-B06, REQ-002-B01, REQ-002-B02, REQ-002-B03, REQ-002-B04, REQ-002-B05, REQ-003-B01, REQ-003-B02, REQ-003-B03, REQ-003-B04, REQ-003-B05, REQ-004-B01, REQ-004-B02, REQ-004-B03, REQ-004-B04, REQ-005-B01, REQ-005-B02, REQ-005-B03, REQ-006-B01, REQ-006-B02, REQ-006-B03, REQ-006-B04]
complexity: medium
---

# Task 6: 端到端回归与结构化证据

## 描述

补充端到端回归测试并生成 verification 所需报告，证明旧 TUI 可续接、不可用时 fallback、安全与观测要求均覆盖。

## 依赖

- 前置 task：T2、T3、T4、T5
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 创建 | `test/claude-tui-reconnect.integration.test.js` | 模拟 Walker stop/restart、旧 sidecar runtime 存活、飞书 prompt 写入同一 runtime 的集成路径。 |
| 创建 | `specs/2026-08-06-claude-tui-reconnect/test-report.md` | 汇总每个 REQ/behavior 的测试命令、结果和 evidence receipt。 |
| 创建 | `specs/2026-08-06-claude-tui-reconnect/verify-report.md` | 汇总最终验证、风险和未覆盖项。 |

## 复杂度

medium，原因：主要是验证与证据闭环，但覆盖全部需求并影响最终 gate。

## 实现步骤

- [ ] 编写集成测试，模拟第一代 Walker 创建 Claude runtime、attach TUI 保持连接、Walker stop 后第二代 Walker 重连同一 sidecar runtime。
- [ ] 验证飞书 prompt 经 dispatcher/driver 写入旧 runtimeId，且不创建第二个 PTY/TUI。
- [ ] 编写 fallback 集成场景，sidecar 不可用时使用新受控 runtime 并持久化新 ref。
- [ ] 运行相关单元测试、集成测试、`npm run lint`、`npm run check`。
- [ ] 更新 `traceability.json` 的 behavior 级 `tests` 和 `evidence` 字段，指向真实测试与报告证据。
- [ ] 写入 `test-report.md` 和 `verify-report.md`，逐项提到所有 REQ。

## 验收映射

| Requirement ID | Behavior ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | ----------- | -------- | -------- | -------- |
| REQ-001 | REQ-001-B01 | prompt 写入旧 runtimeId，而不是新 runtimeId。 | `test/claude-tui-reconnect.integration.test.js` | `旧 TUI 存活时重启后复用旧 runtime` |
| REQ-001 | REQ-001-B02 | 恢复后持久化的 agentRef 反映 reconnected/runtime 可用状态。 | `test/claude-tui-reconnect.integration.test.js` | `旧 TUI 存活时重启后复用旧 runtime` |
| REQ-001 | REQ-001-B03 | 飞书 prompt 在续接完成前不会写入错误或不存在 runtime。 | `test/claude-tui-reconnect.integration.test.js` | `续接完成前不写入 stale runtime` |
| REQ-001 | REQ-001-B04 | 状态或日志包含 reused/reconnected 语义且不含敏感信息。 | `test/claude-tui-reconnect.integration.test.js` | `续接状态脱敏` |
| REQ-001 | REQ-001-B05 | 不会先写入旧失效 runtime 后再更新 ref。 | `test/claude-tui-reconnect.integration.test.js` | `调用顺序原子性` |
| REQ-001 | REQ-001-B06 | 可续接时不调用新建 runtime 分支，不打开新终端。 | `test/claude-tui-reconnect.integration.test.js` | `禁止双 TUI` |
| REQ-002 | REQ-002-B01 | fallback 后 prompt 写入新 runtime。 | `test/claude-tui-reconnect.integration.test.js` | `sidecar 不可用时 fallback` |
| REQ-002 | REQ-002-B02 | 不可连接不会被误判为 active。 | `test/claude-tui-reconnect.integration.test.js` | `sidecar 不可用时 fallback` |
| REQ-002 | REQ-002-B03 | 调用 resumeRuntime 并打开新 attach TUI。 | `test/claude-tui-reconnect.integration.test.js` | `sidecar 不可用时 fallback` |
| REQ-002 | REQ-002-B04 | session.agentRef 与 state store 都更新为新 ref。 | `test/claude-tui-reconnect.integration.test.js` | `fallback 持久化新 ref` |
| REQ-002 | REQ-002-B05 | 错误或日志包含 unavailable/fallback，不包含 token/API key。 | `test/claude-tui-reconnect.integration.test.js` | `fallback 状态脱敏` |
| REQ-003 | REQ-003-B01 | app.stop 不调用用户主动 stop/delete 语义。 | `test/claude-tui-reconnect.integration.test.js` | `Walker stop 保留 runtime` |
| REQ-003 | REQ-003-B02 | 多次 stop 结果稳定。 | `test/claude-tui-reconnect.integration.test.js` | `重复 stop 幂等` |
| REQ-003 | REQ-003-B03 | pending 被 reject 或完成，runtime 可诊断。 | `test/claude-tui-reconnect.integration.test.js` | `shutdown pending 处理` |
| REQ-003 | REQ-003-B04 | 旧 runtime 控制面在新 Walker 中可重新发现。 | `test/claude-tui-reconnect.integration.test.js` | `新 Walker 发现旧 runtime` |
| REQ-003 | REQ-003-B05 | 不调用 kill，且不在可续接路径打开独立 claude --resume 终端。 | `test/claude-tui-reconnect.integration.test.js` | `Walker stop 保留 runtime` |
| REQ-004 | REQ-004-B01 | WebSocket close 后 attach 命令尝试重新 resolve/connect。 | `test/claude-tui-reconnect.integration.test.js` | `attach CLI 断线重连` |
| REQ-004 | REQ-004-B02 | 失败退出码非零且 raw mode 恢复。 | `test/claude-tui-reconnect.integration.test.js` | `attach CLI 持续失败退出` |
| REQ-004 | REQ-004-B03 | 断线恢复窗口内可恢复连接。 | `test/claude-tui-reconnect.integration.test.js` | `attach CLI 断线重连` |
| REQ-004 | REQ-004-B04 | 超过恢复窗口后退出并提示。 | `test/claude-tui-reconnect.integration.test.js` | `attach CLI 持续失败退出` |
| REQ-005 | REQ-005-B01 | 状态字段可区分路径。 | `test/claude-tui-reconnect.integration.test.js` | `续接与 fallback 状态` |
| REQ-005 | REQ-005-B02 | 重启后状态从 stale 更新为 reconnected 或 fallback。 | `test/claude-tui-reconnect.integration.test.js` | `状态更新` |
| REQ-005 | REQ-005-B03 | 旧格式 agentRef 不报错，并按 fallback 路径更新。 | `test/claude-tui-reconnect.integration.test.js` | `旧格式兼容` |
| REQ-006 | REQ-006-B01 | 非本地连接被拒绝。 | `test/claude-tui-reconnect.integration.test.js` | `安全边界` |
| REQ-006 | REQ-006-B02 | 缺失或错误 token 被拒绝。 | `test/claude-tui-reconnect.integration.test.js` | `安全边界` |
| REQ-006 | REQ-006-B03 | JSON 化 agentRef、日志和错误文本不含敏感值。 | `test/claude-tui-reconnect.integration.test.js` | `敏感信息脱敏` |
| REQ-006 | REQ-006-B04 | 错误说明 unauthorized 或 invalid token 且脱敏。 | `test/claude-tui-reconnect.integration.test.js` | `安全边界` |

## Traceability 更新要求

- planning 阶段：把本 task 的 `requirements` 和 `behavior_ids` 写入 `traceability.json` 的 `tasks` 字段。
- executing 阶段：补齐对应 behavior 的 `tests` 和 `evidence` 引用。
- 不允许只映射到 REQ 而遗漏 behavior 级映射。

## TDD 步骤

- [ ] 写失败测试：`test/claude-tui-reconnect.integration.test.js` — Walker 重启后旧 TUI 存活时飞书 prompt 进入旧 runtime。
- [ ] 写失败测试：`test/claude-tui-reconnect.integration.test.js` — sidecar 不可用时 fallback 并持久化新 ref。
- [ ] 运行确认失败：`node --test "test/claude-tui-reconnect.integration.test.js"`
- [ ] 实现或补齐集成路径通过测试
- [ ] 运行全量验证并更新报告

## 测试说明

- 测试文件：`test/claude-tui-reconnect.integration.test.js`
- 覆盖场景：跨 Walker 重启复用旧 runtime、fallback、安全边界、观测状态、shutdown 幂等、attach CLI 重连
- 验证命令：`node --test "test/claude-tui-reconnect.integration.test.js"`、`npm run lint`、`npm run check`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
