---
owns: [src/drivers/opencode-driver.js]
reads: [src/drivers/agent-driver.js]
depends_on: [T1]
requirements: [REQ-017]
complexity: low
---

# Task 3: OpencodeDriver.replyPermission 方法

## 描述

在 OpencodeDriver 类中新增 replyPermission 方法，调用 OpenCode server 的 POST /session/:id/permissions/:permissionId 端点发送权限回复。

## 依赖

- 前置 task：T1（TYPE 常量，仅类型引用）
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/drivers/opencode-driver.js` | 新增 replyPermission 方法 |
| 修改 | `test/opencode-driver.test.js` | 新增 replyPermission 测试 |

## 复杂度

low，原因：单一 HTTP POST 方法，参照现有 stop/delete 方法的模式实现

## 实现步骤

- [ ] 步骤 1：在 OpencodeDriver 类中（stop 方法附近）新增 `replyPermission(sessionRef, permissionId, response, remember)` 方法
- [ ] 步骤 2：方法逻辑：
  - 校验 `sessionRef.opencodeSessionId` 和 `permissionId` 存在
  - TUI bridge 场景处理（若 `_isTuiBridge` 则抛错或走 tuiBridge）
  - 构建 URL：`/session/<opencodeSessionId>/permissions/<permissionId>`
  - body：`{ response: response, remember: remember !== undefined ? remember : false }`
  - 调用 `this.httpClient.request('POST', url, body)`
  - 成功记录 info 日志，失败记录 warn 日志并抛出
- [ ] 步骤 3：参照现有 stop 方法（opencode-driver.js:478）的模式实现

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-017 | 给定 sessionRef 和 permissionId 和 response，当 replyPermission 调用时，则向 OpenCode server 发送 POST /session/:id/permissions/:permissionID，body 为 { response, remember?: false } | `src/drivers/opencode-driver.js:replyPermission` | `test/opencode-driver.test.js` — "replyPermission 发送 POST" |

## TDD 步骤

- [ ] 写失败测试：`test/opencode-driver.test.js` — mock httpClient，调用 replyPermission，验证 URL 和 body 正确
- [ ] 运行确认失败：`npm test -- --grep "replyPermission"`
- [ ] 实现通过测试
- [ ] 重构（如需要）

## 测试说明

- 测试文件：`test/opencode-driver.test.js`
- 覆盖场景：正常调用 URL/body 验证、缺少 sessionRef 抛错、缺少 permissionId 抛错、HTTP 失败抛错
- 验证命令：`npm test`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
