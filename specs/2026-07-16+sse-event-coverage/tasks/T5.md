---
owns: [src/platform/feishu/commands.js]
reads: []
depends_on: []
requirements: [REQ-018]
complexity: low
---

# Task 5: /permit 命令注册

## 描述

在 commands.js 的 COMMANDS 表中注册 /permit 命令，使 parseCommand 能识别并解析参数。

## 依赖

- 前置 task：无
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/platform/feishu/commands.js` | COMMANDS 表新增 permit 条目 |
| 修改 | `test/feishu-commands.test.js` | 新增 /permit 解析测试 |

## 复杂度

low，原因：单条命令注册，参照现有命令模式

## 实现步骤

- [ ] 步骤 1：在 COMMANDS 对象中新增 `permit` 条目：
  - `permit: { desc: '回复权限确认请求', usage: '/permit <permissionId> <allow|deny>' }`
- [ ] 步骤 2：无需修改 parseCommand 函数（已通过 COMMANDS[name] 查找自动识别）
- [ ] 步骤 3：无需修改 formatHelp（自动从 COMMAND_LIST 生成）

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-018 | 给定用户输入 /permit 命令，当 parseCommand 解析时，则识别为 permit 命令，参数为 permissionId 和 allow/deny | `src/platform/feishu/commands.js:COMMANDS` | `test/feishu-commands.test.js` — "/permit 命令解析" |

## TDD 步骤

- [ ] 写失败测试：`test/feishu-commands.test.js` — 验证 `parseCommand('/permit perm_abc allow')` 返回 `{ type: 'command', name: 'permit', args: ['perm_abc', 'allow'] }`
- [ ] 运行确认失败：`npm test -- --grep "permit"`
- [ ] 实现通过测试
- [ ] 重构（如需要）

## 测试说明

- 测试文件：`test/feishu-commands.test.js`
- 覆盖场景：/permit 正常解析、缺少参数时仍解析为命令（参数校验在 dispatcher 层）、/permit deny 解析
- 验证命令：`npm test`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
