---
owns: [src/app/bootstrap.js]
reads: [src/platform/feishu/cards.js, src/platform/feishu/progress-card.js, src/dispatch/message-dispatcher.js]
depends_on: [T4, T6]
requirements: [REQ-002, REQ-003]
complexity: low
---

# Task 7: bootstrap.js API 绑定扩展

## 描述

在 bootstrap.js 的 feishuApiTarget 绑定区新增 sendPermissionCard 和 patchPermissionCard 方法绑定，并确保 requiredFeishuMethods 列表包含新增方法。

## 依赖

- 前置 task：T4（buildPermissionCard 函数）、T6（MessageDispatcher 权限处理逻辑）
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | `src/app/bootstrap.js` | 新增 feishuApiTarget 方法绑定 |
| 修改 | `test/bootstrap.test.js` | 新增绑定验证测试 |

## 复杂度

low，原因：参照现有 feishuApiTarget 绑定模式添加 2-3 个方法绑定

## 实现步骤

- [ ] 步骤 1：在 bootstrap.js 的 feishuApiTarget 绑定区（约 175-224 行之间）新增：
  - `feishuApiTarget.sendPermissionCard = (replyCtx, permissionEvent, sessionId, routeKey) => platform.api.replyCard(normalizeReplyCtx(replyCtx), buildPermissionCard(permissionEvent, sessionId, routeKey))`
  - `feishuApiTarget.patchPermissionCard = (cardId, permissionId, response) => platform.api.patchCard(cardId, buildPermissionRepliedCard(permissionId, response))`
- [ ] 步骤 2：在 requiredFeishuMethods 数组（约 226-230 行）中新增 `'sendPermissionCard'` 和 `'patchPermissionCard'`
- [ ] 步骤 3：确保从 cards.js 导入 buildPermissionCard 和 buildPermissionRepliedCard

## 验收映射

| Requirement ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | -------- | -------- | -------- |
| REQ-002 | 权限卡片通过 feishuApiTarget.sendPermissionCard 发送 | `src/app/bootstrap.js:feishuApiTarget` | `test/bootstrap.test.js` — "sendPermissionCard 绑定" |
| REQ-003 | 权限回复通过 feishuApiTarget.patchPermissionCard 更新 | `src/app/bootstrap.js:feishuApiTarget` | `test/bootstrap.test.js` — "patchPermissionCard 绑定" |

## TDD 步骤

- [ ] 写失败测试：`test/bootstrap.test.js` — 验证 feishuApiTarget.sendPermissionCard 和 patchPermissionCard 为函数
- [ ] 运行确认失败：`npm test -- --grep "bootstrap.*permission"`
- [ ] 实现通过测试
- [ ] 重构（如需要）

## 测试说明

- 测试文件：`test/bootstrap.test.js`
- 覆盖场景：sendPermissionCard/patchPermissionCard 方法存在且为函数、调用时正确转发到 platform.api
- 验证命令：`npm test`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
