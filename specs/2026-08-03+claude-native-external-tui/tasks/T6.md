---
owns: []
reads:
  - src/drivers/opencode-driver.js
  - src/opencode-tui-bridge/
  - test/opencode-driver.test.js
  - test/opencode-tui-bridge.test.js
  - test/runtime.test.js
depends_on:
  - T1
  - T2
  - T3
  - T4
  - T5
requirements: [REQ-007]
behavior_ids: [REQ-007-B01, REQ-007-B02, REQ-007-B03]
behaviors:
  - REQ-007-B01
  - REQ-007-B02
  - REQ-007-B03
complexity: medium
---

# Task 6: OpenCode 零变更与最终回归

## 描述

执行 OpenCode 保护边界和最终回归验证，确认本次 Claude PTY 改造没有修改 OpenCode 专属源码、测试或 `WindowsRuntime.openTerminal()` 旧语义。

## 依赖

- 前置 task：T1、T2、T3、T4、T5
- 外部依赖：无

## 涉及文件

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 读取 | `src/drivers/opencode-driver.js` | 确认未修改 |
| 读取 | `src/opencode-tui-bridge/` | 确认未修改 |
| 读取 | `test/opencode-driver.test.js` | 运行既有回归 |
| 读取 | `test/opencode-tui-bridge.test.js` | 运行既有回归 |
| 读取 | `test/runtime.test.js` | 验证 `openTerminal()` 旧语义和 Claude 专用新增方法隔离 |

## 复杂度

medium，原因：该任务不写源码，但需要机械检查 diff、运行定向和全量测试并记录证据。

## 实现步骤

- [ ] 步骤 1：运行 `git diff --name-only`，确认不包含 OpenCode 专属源码和测试路径。
- [ ] 步骤 2：运行 OpenCode 定向测试：`node --test test/opencode-driver.test.js test/opencode-tui-bridge.test.js test/opencode-http-client.test.js test/runtime.test.js`。
- [ ] 步骤 3：运行 Claude 定向测试：`node --test test/claude-pty-broker.test.js test/claude-attach.test.js test/cli-claude-attach.test.js test/claude-driver.test.js test/claude-transcript.test.js test/message-dispatcher.test.js test/runtime.test.js`。
- [ ] 步骤 4：运行 `git diff --check` 和 `npm test`。
- [ ] 步骤 5：把测试命令、结果和 diff 范围写入 executing/verification 阶段要求的报告。

## 验收映射

| Requirement ID | Behavior ID | 验收标准 | 代码位置 | 测试用例 |
| -------------- | ----------- | -------- | -------- | -------- |
| REQ-007 | REQ-007-B01 | OpenCode相关测试全部通过 | 既有OpenCode代码 | `test/opencode-driver.test.js`、`test/opencode-tui-bridge.test.js` |
| REQ-007 | REQ-007-B02 | git diff文件清单不包含OpenCode专属源码与测试 | Git diff | `git diff --name-only` |
| REQ-007 | REQ-007-B03 | openTerminal旧语义不变且新能力独立通过 | `src/runtime/windows-runtime.js` | `test/runtime.test.js` |

## Traceability 更新要求

- planning 阶段：把本 task 的 `requirements` 和 `behavior_ids` 写入 `traceability.json` 的 `tasks` 字段。
- executing 阶段：补齐对应 behavior 的 `tests` 和 `evidence` 引用。
- 不允许只映射到 REQ 而遗漏 behavior 级映射。

## TDD 步骤

- [ ] 写失败测试：本任务不新增源码测试，通过执行既有 OpenCode 测试和 diff 机械检查提供保护。
- [ ] 运行确认失败：若前序任务破坏 OpenCode或runtime语义，定向命令必须失败。
- [ ] 实现通过测试：由前序任务修复破坏项。
- [ ] 重构：不适用，除非验证发现 Claude 新代码越界影响 OpenCode。

## 测试说明

- 测试文件：`test/opencode-driver.test.js`、`test/opencode-tui-bridge.test.js`、`test/opencode-http-client.test.js`、`test/runtime.test.js`
- 覆盖场景：OpenCode session、TUI bridge、HTTP client、Windows runtime旧方法兼容。
- 验证命令：`node --test test/opencode-driver.test.js test/opencode-tui-bridge.test.js test/opencode-http-client.test.js test/runtime.test.js`、`git diff --name-only`、`npm test`

## 完成标准

- [ ] 所有步骤完成
- [ ] 测试通过
- [ ] 无占位符残留
